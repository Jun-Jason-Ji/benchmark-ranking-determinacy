# Generated: implementation-build drift on a paired episode scope

Generated by `scripts/analyze_platform_drift_paired.py`.

Build drift is a paired quantity: same seeds, different build. With 64 configurations,
episode ids beyond the grid wrap onto it, so the two builds must be compared on the episode
ids they share, one observation per configuration on each side. The naive column shows what
an unpaired per-configuration mean over each directory's full contents gives instead.

| policy | condition | pre eps | post eps | shared | naive drift | **paired drift** |
|---|---|---:|---:|---:|---:|---:|
| octo-small | `nominal` | 96 | 64 | 64 | +0.055 | **+0.031** |
| octo-small | `force_x0.5` | 96 | 64 | 64 | +0.047 | **+0.016** |
| octo-base | `nominal` | 96 | 64 | 64 | +0.023 | **+0.078** |
| octo-base | `force_x0.5` | 96 | 64 | 64 | +0.039 | **+0.047** |

Maximum paired drift: **0.078**. Mean **0.043**.
Maximum naive drift: 0.055 (mean 0.041).

Those are changes in a SINGLE policy's success rate. The manuscript's Table 3 reports a quantity in units of the policy difference instead, because a single-policy rate change is not an effect on a ranking and does not belong on a Delta axis. The shift in Delta = rate(octo-small) - rate(octo-base) is:

| condition | Delta before | Delta after | shift in Delta |
|---|---:|---:|---:|
| `nominal` | +0.2500 | +0.1406 | **-0.1094** |
| `force_x0.5` | +0.1875 | +0.1250 | **-0.0625** |

Maximum |shift in Delta|: **0.1094**, mean 0.0859. This is the figure Table 3 carries; the 0.078 above is the largest single-policy rate change and was the wrong statistic for that table.

## The cell FINDING_platform_drift.md quotes

- recorded: pre 0.297 vs post 0.375, drift 0.078
- paired on the 64 shared episode ids: pre 0.297 vs post 0.375, drift 0.078
- naive over the full pre-fix directory (96 episodes): 0.352

If the paired figure reproduces 0.297/0.078, the recorded number was right and the later
recomputation was the unpaired one. The paper should then state the paired value and say
explicitly that it is paired on the shared ids.
