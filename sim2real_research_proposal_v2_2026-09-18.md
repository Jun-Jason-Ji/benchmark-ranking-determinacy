# 决策相关可辨识性：校准仿真何时能可靠排序机器人基础策略

研究方案 v2，2026-09-18。基于 2026-09-18 初版按 TOP 期刊标准审计后重写；审计报告见 `sim2real_proposal_audit_2026-09-18.md`。面向具备 GPU、暂无自有机器人的研究者。默认研究周期 9–12 个月至首次投稿。

本文件是待实验检验的研究设计，不是已完成的实验或录用承诺。文献、数据集与平台信息按 2026-09-18 检索时的公开资料整理；未下载全量数据或实测代码。任务规模、性能目标与资源预算均为计划值。

---

## 0. 一页摘要

**科学命题。** 用少量真实轨迹校准仿真、再在仿真中比较机器人策略，是当前评估视觉语言动作（VLA）基础模型的主流做法。我们提出并检验一个命题：**物理参数的可辨识性与策略排序的可判定性互不蕴含。** 校准轨迹（通常是人类遥操作示范）激励的物理方向，与被比较策略敏感的物理方向常常不一致；因此存在大量“轨迹拟合同样好、策略排名却相反”的物理解释，而单点校准与参数分布校准都会掩盖它。

**方法。** 从真实证据构造名义水平 (1−α) 的相容物理集合，把集合传播到同场景、同参数下的策略差值，输出可判定的部分序并对不可判定的策略对显式拒判；再以“单位成本增加的可判定策略对数”为目标选择下一条真实证据。

**验证。** 三条零硬件真机通道：SIMPLER 生态（公开检查点 + 真机成功率 + 匹配真实轨迹）作为完整闭环；AutoEval 公开 WidowX 评测队列作为真实探测与新策略验证通道；RoboArena 策略提交作为第二平台外部验证。

**四条贡献。**
- C1 概念与理论：定义 δ-可判定性；给出不可辨识反例、条件排序正确性、以及“激励失配”判据。
- C2 可计算证书：集合构造、差值传播、共享集合的同时有效性、结构失配报警。
- C3 面向可判定性的证据选择：与参数熵／D-最优／误差优先的对照。
- C4 经验图谱与复现包：VLA 策略在多任务上的歧义普遍性统计，含真机成对验证，发布冻结的歧义案例集。

---

## 1. 题目与定位

**中文题目：决策相关可辨识性：校准仿真何时能可靠排序机器人基础策略。**

**英文题目：Decision-Relevant Identifiability: When Can Calibrated Simulation Rank Robot Foundation Policies?**

论文属于 Real2Sim 校准支撑的 Sim2Real 可信评估。评测对象是 VLA 基础模型，方法本身对策略类型不敏感。“大模型”在论文中只体现为被评估的策略，不使用 VLM 作为事件提取器或物理先验来源；若后续需要语义事件标签，作为附录可选接口处理。

## 2. 核心概念

设联合未知量 z = (θ, ν, κ)：θ 为动力学参数（接触摩擦、阻尼、执行时延、控制增益），ν 为观测／相机参数，κ 为控制接口参数。真实证据 D 为一组含图像、动作、本体状态与时间戳的轨迹。任务上下文 c 含指令、场景、机器人型号与控制协议。

**相容集合。** Z_α(D) = { z : L(D, z) ≤ ε_α，且满足物理与标定约束 }，其中 ε_α 由名义水平 α 决定（见 5.2）。

**策略差值。** Δ_ij(z, c) = J_sim(π_i; z, c) − J_sim(π_j; z, c)，J 为共同场景分布下的成功率或进度。

**δ-可判定性。** 给定实践差异阈值 δ ≥ 0，策略对 (i, j) 在 D 下 δ-可判定，当且仅当 inf_{z∈Z_α(D)} Δ_ij > δ 或 sup_{z∈Z_α(D)} Δ_ij < −δ。否则该对不可判定，方法拒判。

**命题（非正式）。** 令 I_D 为校准数据关于 z 的信息矩阵，g_ij = ∇_z Δ_ij。(a) 若 g_ij 落在 I_D 的零空间附近，则 θ 的整体不确定度再小也不能保证可判定：这是“参数拟合好、排名不可靠”的成因。(b) 若 g_ij 与 I_D 的主方向对齐，则 θ 整体不可辨识也可能可判定。(c) 增加与现有数据同分布的校准轨迹，只压缩 I_D 已有方向上的不确定度，对 (a) 型策略对无效；这解释了为什么“更多示范数据”不等于“更可靠的排名”，也是证据选择的动机。

## 3. 与近邻工作的差异

### 3.1 机器人 Sim2Real 与策略评估

| 工作 | 歧义表示 | 辨识目标 | 拒判 | 证据选择 | 真机成对验证 |
|---|---|---|---|---|---|
| [DROPO](https://arxiv.org/abs/2201.08434) 2022/2023 | 参数分布 | θ | 无 | 无 | 单任务 |
| [ASID](https://arxiv.org/abs/2404.12308) 2024 | 点估计 | θ | 无 | 主动探索，目标为参数信息 | 有 |
| [SIMPLER](https://arxiv.org/abs/2405.05941) 2024 | 点估计 | 视觉匹配 + 控制器 | 无 | 无 | 有，聚合成功率 |
| [Sim2Val](https://nvlabs.github.io/sim2val/) 2025 | 配对残差 | 真实表现估计 | 无 | 无 | 有 |
| [PolaRiS](https://polaris-evals.github.io/) 2025 | 点估计 | 场景重建 | 无 | 无 | 有 |
| [Real-is-Sim](https://arxiv.org/abs/2504.03597) 2025 | 在线同步孪生 | 状态 | 无 | 无 | 有，需在场机器人 |
| [SCAPE](https://arxiv.org/abs/2608.19425) 2026.08 | conformal 区间 | 场景条件表现 | 区间 | 无 | 有 |
| [R2S-Eval](https://arxiv.org/abs/2609.03276) 2026.09 | 点估计 | 校准 + VLM 偏好 | 无 | 无 | 有 |
| **本方案** | (1−α) 相容集合 | **Δ 的符号** | **部分序 + 拒判** | **以可判定性为目标** | 三通道 |

### 3.2 必须承认的理论近邻

- **集合成员辨识**（Milanese & Vicino 1991；Fogel & Huang 1982）：Z_α 就是可行参数集。本方案不声称集合构造是新的，新意在集合到策略差值符号的传播与决策规则。
- **面向控制的辨识 I4C**（Gevers 1993、2005；Van den Hof & Schrama 1995）：“只辨识对控制目标重要的部分”。本方案是其在“不可解析、只能 rollout 的闭环视觉策略集合比较”上的对应物，且处理的是比较而非设计。
- **值等价原理**（Grimm et al. 2020；2021）：模型只需对给定策略集在值上等价。本方案研究其对偶：数据无法区分、但值不等价的模型集合。
- **决策导向学习**（Elmachtoub & Grigas 2022）：以决策损失训练预测器。本方案不训练预测器，而是判定决策是否被证据支撑。
- **置信离线策略选择**（Kuzborskij et al. 2021；Paine et al. 2020；BayesDICE 2022）：在重要性权重空间做置信选择，需行为策略概率与覆盖。本方案在低维物理参数空间做部分辨识，不需要行为策略概率，但需要仿真器。
- **部分辨识与稳健策略评估**（Kallus & Zhou 2020 等）：本方案是其在接触物理参数上的具体化。

**创新边界声明。** 本方案不声称首创部分辨识、集合成员估计、拒判或主动辨识。声称的新意是：(i) 决策可辨识性与参数可辨识性的解耦及其激励失配解释；(ii) 面向 VLA 评估的可计算证书与同时有效的部分序；(iii) 以可判定性为目标的证据选择；(iv) 在公开真机通道上的成对验证与歧义普遍性图谱。最终新颖性仍需在写作时结合持续文献跟踪确认。

## 4. 研究问题与可证伪假设

**RQ0 普遍性：** 在现有 VLA 评估基准上，排序不可判定与排名翻转有多常见，与激励失配指标是否相关？
H0：在 SIMPLER 全部任务 × 策略对上，名义水平 α = 0.1 的相容集合内出现符号翻转的策略对比例不低于 15%；翻转比例与校准数据在 g_ij 方向上的信息量负相关。若翻转比例低于 5% 且与激励失配无关，则该现象不足以支撑论文，转向 13 节的收缩方案。

**RQ1 拟合误差 vs. 排序可靠性：** 低轨迹拟合误差是否足以支撑可靠排序？
H1：在受控合成实验中，单点拟合误差与真实目标环境排序错误率的相关性弱于集合宽度与排序错误率的相关性。测试集随机生成、预先冻结，不用精挑案例。

**RQ2 保留歧义并拒判 vs. 强制全排名：** 在公平的拒判设置下，歧义感知方法是否在风险—覆盖率曲线上占优？
H2：在相同覆盖率（给出结论的策略对占比）下错误排序率更低；在相同错误率下可判定策略对更多。所有基线配备同一名义水平的拒判规则（见 10 节）。

**RQ3 证据选择：** 以可判定性为目标选择真实证据，是否比以参数信息为目标更快降低排序歧义？
H3：同样的证据预算下，可判定策略对数的增长曲线优于随机、均匀覆盖、误差优先、参数熵、D-最优。离线日志选择与 AutoEval 真实探测两条赛道分别报告。

**RQ4 真机有效性：** 方法在真机上给出的可判定结论是否正确、拒判是否对应真机上确实难分的策略对？
H4：可判定结论与真机排序的一致率不低于 1−α−β 减去经验偏差项；被拒判的策略对中，真机成功率差的置信区间跨过 δ 的比例显著高于可判定对。

## 5. 方法

### 5.1 观测处理与参数分离
先核对动作单位、控制模式、频率与时序，再研究接触物理；否则相机偏移或协议错误会被吸收进摩擦或质量。观测摘要用机器人轨迹、可跟踪物体位移、接触起止、滑移与停止时间，全部由几何跟踪产生。ν、κ 可做 profile 优化或边缘化；若工程上冻结，结论必须标注“条件于该标定”。

### 5.2 相容集合的构造与阈值的统计语义
损失 L(D, z) 为按观测噪声尺度归一化的轨迹残差；权重对所有方法一致。阈值 ε_α 由以下之一给出，并在预注册中固定：
- 轮廓似然：ε_α = min_z L + q_{1−α}，q 由校准集上留一轨迹的经验分位数给出；高斯噪声下可用 χ² 近似作对照。
- 分裂 conformal：在留出轨迹上校准残差分位数。

集合表示：多起点优化保留多模式，SMC／受约束采样填充，4–6 维参数下辅以有限元素网格核验。有限样本极值只是敏感性范围，不是连续空间上的认证界；低维可验证例子用区间分析或分支定界给出认证版本作对照。

**结构失配报警。** 若 min_z L 超过留出校准的拟合优度阈值，输出“模型结构或观测假设不相容”，不给出任何排序结论。

### 5.3 差值传播与同时有效性
对每个 z 用共同随机数（同一初始条件与策略采样种子）估计 Δ_ij(z)，构造同时置信带 [Δ⁻, Δ⁺]；再取 L_ij = inf Δ⁻，U_ij = sup Δ⁺，避免对噪声函数直接取极值带来的乐观偏差。参数维度 ≤ 6 时以高斯过程响应面替代逐点 rollout，主动采样集中在集合边界。

**同时有效性。** 所有策略对共享同一 Z_α(D)：若真实系统被集合覆盖，则全部成对结论在参数不确定性这一层同时成立于水平 1−α，不需逐对多重比较修正；Monte Carlo 层由同时置信带处理。这是相对逐对检验的结构性优势，须在论文中明确。

### 5.4 仿真结构偏差 b 的估计
两条路线都做：
1. 用 A 级配对数据（同一策略、同一任务的仿真成功率与真机成功率）估计差值残差 r = Δ_real − Δ_sim 的分布，以其 (1−β) 分位数作为经验 b_emp，区间扩展为 [L−b_emp, U+b_emp]。这是 Sim2Val 控制变量思想在差值上的应用。
2. 报告区间对真机差值的实际覆盖率，作为诊断而非保证。

没有配对数据的任务只报告经验敏感性范围，并在结论中降级。

### 5.5 决策规则与输出
L_ij − b > δ ⇒ π_i 更好；U_ij + b < −δ ⇒ π_j 更好；否则拒判。输出部分序及每个拒判对的“翻转证据”：两组解释 z⁺、z⁻ 的拟合误差与预测视频、导致差异的物理事件、能区分二者的证据类型。渲染差异与策略随机性导致的差异分别归因。

### 5.6 面向可判定性的证据选择
候选证据池 E（离线日志或 AutoEval 可执行的探测动作）。对每个候选 e，用当前集合下的预测分歧估计其揭示后集合收缩对可判定策略对数的期望增量 V(e)，按成本归一化选择 argmax V(e)/cost(e)，揭示后更新集合。基线：随机、均匀覆盖、误差优先、参数熵、D-最优。所有方法使用相同的可见元数据与预算；离线赛道只声称节省标注／获取成本，真实赛道（AutoEval 执行）才称为主动探索。

## 6. 理论部分

1. **不可辨识反例。** 构造两套动力学，对行为策略覆盖的动作产生相同观测分布，对目标策略对产生相反差值。给出平面推物中的显式实例：摩擦与执行时延的耦合在慢速示范下不可分，在快速策略下决定成败。
2. **条件排序正确性。** 若真实系统 z* ∈ Z_α(D)、Monte Carlo 误差由同时置信带控制、结构偏差由 b 覆盖，则所有可判定结论同时正确，概率不低于 1−α−β−γ。
3. **激励失配判据。** 线性高斯代理下：策略对 δ-可判定需要 g_ij^T I_D^{-1} g_ij ≤ (δ/q_α)²；若 g_ij 在 I_D 零空间的投影非零，则同分布数据量趋于无穷也不能满足。给出所需“定向信息量”的下界，并说明证据选择的目标函数与之对应。
4. **单调性（作为备注）。** 嵌套集合下真实极值区间不扩大；重新拟合“平均损失 ≤ 固定阈值”不保证嵌套，需逐批约束交集；近似采样算法不满足单调性时单独讨论。

理论用于解释现象与界定方法适用条件，不作为全文的主要支撑。

## 7. 任务范围

先做短时程刚体操作。

| 任务族 | 平台 | 优先歧义 | 为什么影响策略比较 |
|---|---|---|---|
| 平面推物／移近 | SIMPLER Google Robot | 摩擦、接触点、动作增益、时延 | 快推与慢推在不同参数下交换优劣 |
| 抓取后放置 | SIMPLER WidowX、AutoEval 水槽任务 | 夹爪接触参数、控制器增益、相机外参 | 抓取稳定性与落点误差 |
| 开合抽屉 | SIMPLER Google Robot、AutoEval 抽屉任务 | 阻尼、关节摩擦、初始状态 | 大动作与谨慎动作表现不同 |

参数从 4–6 维开始：接触摩擦、控制增益、时延、初始位姿偏差。质量、惯量等只有在输入／标定足以支持时加入。位置控制下由视觉拟合得到的“等效参数”不当作物理真值。

## 8. 数据与真机通道

| 来源 | 公开确认的内容 | 本方案用途 | 首月须核验 |
|---|---|---|---|
| [SIMPLER](https://github.com/simpler-env/SimplerEnv) | Google Robot 与 WidowX 环境；RT-1 系列／RT-1-X／Octo 真机成功率；控制器辨识流程 | **第一条完整闭环**：仿真 + 公开检查点 + 真机标签 | **2026-09-18 已核验**：RT-1-X zip（775 MB）与 RT-1 converged／15%／begin 三个目录在公开 GCS 桶中完整可下；Octo base／small 及 1.5 版在 HuggingFace 可访问；REAL_PERF 位于 `simpler_env/utils/metrics.py`，含 5 个 Google Robot 任务 × 6 策略与 4 个 WidowX 任务 × 3 策略；仓库最近提交 2025-12-20。**待核验**：REAL_PERF 中 rt-2-x 无公开检查点，只能作为真机标签不能重跑；各条目的真机试验次数需从论文附录取 |
| [BridgeData V2](https://rail-berkeley.github.io/bridgedata/) | WidowX 真实轨迹，含图像、动作、状态 | WidowX 环境的真实校准轨迹 | 与 SIMPLER 场景／物体匹配的子集规模 |
| Open X-Embodiment 中的 fractal 数据 | Google Robot 真实轨迹 | Google Robot 环境的真实校准轨迹 | 同上 |
| [AutoEval](https://auto-eval.github.io/) | 两台公开 WidowX 站；开抽屉、关抽屉、茄子入篮、茄子入水槽四个任务；策略以 REST 服务器形式提交（输入 256×256 RGB + 8 维本体状态 + 指令，输出 7 维末端增量动作）；日志公开在 HuggingFace | **真实探测通道**：执行算法选择的探测动作；新策略的真机验证 | **2026-09-18 已核验，状态存疑**：官网声明四个任务“开放至 2026-01-01，2026 年任务待定”，官网仓库最后更新 2025-07-14；提交入口 auto-eval.ngrok.app/page 两次探测均返回 ERR_NGROK_3200（端点离线）；但评测日志数据集最近更新 2026-06-14，代码仓库最近提交 2026-03-26，说明今年上半年仍在运行。**行动**：第 1 周直接联系作者团队确认 2026 年开放计划与任务；在得到确认前，实验 D 真实赛道按“条件项”处理，不作为必做项 |
| [RoboArena](https://arxiv.org/abs/2506.18123) 网络与 [数据快照](https://huggingface.co/datasets/RoboArena/DataDump_07-17-2026) | DROID 平台双盲成对评测；官方称网络已对社区开放；快照含视频、状态／动作、结果与偏好 | **第二平台外部验证**：提交策略获取真机成对结果；历史快照做留出分析 | 提交流程与门槛；快照的 session 去重与时间戳 |
| [DROID](https://droid-dataset.github.io/droid/the-droid-dataset.html) | Franka 平台真实示范，多视角 | RoboArena 平台的真实校准轨迹 | 与 PolaRiS 重建场景的匹配 |
| [PolaRiS](https://github.com/arhanjain/polaris) | DROID 风格重建场景、评分定义、部分检查点 | RoboArena 赛道的仿真侧 | 检查点是否与 RoboArena 历史评估的权重版本一致 |

**生态对齐。** WidowX 赛道：BridgeData V2（校准）+ SIMPLER WidowX（仿真）+ AutoEval（真机）。Google Robot 赛道：fractal（校准）+ SIMPLER Google Robot（仿真）+ 历史真机成功率。Franka 赛道：DROID（校准）+ PolaRiS（仿真）+ RoboArena（真机）。三条赛道各自内部对齐，不跨平台混用参数。

**已知限制。** SIMPLER 真机标签是聚合成功率，用于排序验证足够，不能用于逐次事件对齐；RoboArena 快照是累积数据，须按 session 去重并按时间戳划分；多视角视频属同一 episode；策略权重版本、动作空间、控制频率、初始条件必须逐项匹配，否则不称为同一策略的成对比较。

## 9. 首月门槛

1. 三条通道核验表：可下载检查点数、真机标签条目数与试验次数、校准轨迹匹配子集规模、AutoEval 与 RoboArena 的可提交状态。
2. 在 SIMPLER 一个任务上跑通默认仿真、单点校准、参数分布校准，复核动作协议。
3. 产出图 1 的第一个版本：同一段真实轨迹、两个拟合误差相同的解释、两个策略在两种解释下成功率交叉。
4. 冻结预注册文档（假设、指标、ε 选择程序、δ、测试集合）。

**继续条件。** 至少一条赛道达到 A 级（仿真环境可匹配、≥ 3 个可重跑策略、真机标签可用），且图 1 现象在随机生成的情境中可复现。若 AutoEval 与 RoboArena 均不可用，方法与 SIMPLER 闭环仍可支撑论文，但主张范围收缩至“历史真机标签上的外部验证”，RQ3 只保留离线赛道。

## 10. 基线与公平拒判

| 基线 | 拒判规则（同一 δ 与名义水平） |
|---|---|
| 默认仿真 | 无参数不确定性；仅 Monte Carlo 置信带跨 δ 时拒判 |
| 标准域随机化 | 随机化分布下 Δ 分位数区间 |
| 单点 SysID（CMA-ES／退火） | 校准轨迹自助重采样得到 Δ 区间 |
| DROPO／BayesSim 类分布 | 后验采样 Δ 分位数区间 |
| SIMPLER 原评估 | 同默认仿真 |
| 纯数据结果预测器 | conformal 预测区间 |
| Sim2Val／SCAPE | 各自的预测区间 |
| R2S-Eval | Bradley–Terry 参数的置信区间；官方代码不可用时自行实现并标注 |

匹配真实证据数、标注成本、仿真调用数、策略版本、超参数搜索预算。依赖真实结果标签的方法与只依赖动力学轨迹的方法分两条赛道报告。

## 11. 实验

**实验 A 受控机制验证（合成，已知真值）。** 3 个任务族 × 10–20 个参数情境，控制激励不足、遮挡、时延、接触耦合。报告：拟合误差 vs. 排序错误、集合宽度 vs. 排序错误、激励失配指标 vs. 翻转发生。同引擎不同参数称参数迁移；不同引擎／接触模型称结构失配实验。

**实验 B 歧义图谱（RQ0）。** SIMPLER 全部任务 × 策略对，以 BridgeData V2 / fractal 的匹配子集校准，统计可判定比例、翻转比例、集合宽度分布，并与激励失配指标回归。发布冻结的歧义案例集。

**实验 C 真机成对验证（RQ4）。** SIMPLER 历史真机成功率作为外部标签；固定校准集、冻结方法后再打开真机结果。按任务分层；真机标签自身不确定性用 Wilson 区间，区间跨 δ 的策略对标为“真机未决”，不计入错误。若 RoboArena 可提交，以 PolaRiS + DROID 赛道重复。

**实验 D 真实探测（RQ3 真实赛道）。** 在 AutoEval 上固定候选策略与有限探测动作集，比较无额外交互、随机探测、参数熵探测、D-最优探测、可判定性探测。报告真实交互次数、可判定对数增长、真机排序一致率。本方案尚未提交任何作业；任务与动作授权、平台规则须事先确认。

**实验 E 泛化与失配。** 未见场景、物体、策略家族、时间段分别留出，最后组合留出。有意加入候选空间不能解释的现实差异（额外时延、接触模型改变、相机漂移），检验方法是否转为不确定或触发结构失配报警。

**实验 F 消融。** 单点／分布／集合；ν、κ 与 θ 分离；同参数差值／独立分数区间；拒判机制；响应面代理／逐点 rollout；b 估计的两条路线；ε 的两种校准方式。

## 12. 指标与统计

主指标：
1. 固定覆盖率下的错误排序率与风险—覆盖率曲线面积。
2. 固定错误率下可判定策略对比例。
3. 选型遗憾值；拒判对应的真机复核成本按 AutoEval 实际用时计入。
4. 达到同一可靠度所需真实证据／真机交互次数。
5. **真机试验等价节省**：为达到同样的排序错误率，纯真机评测需要的 episode 数与本方法需要的 episode 数之比。

辅助：Kendall τ、MMRV、区间宽度与实际覆盖率、事件／轨迹误差、GPU 时间、仿真调用数。策略数少时不用 Spearman/Pearson 作结论。

统计：参数层由共享集合保证同时有效；Monte Carlo 层用同时置信带；真机标签层对 session／场景做 cluster bootstrap，聚合均值缺少样本量时不伪造 episode。3–5 个随机种子；策略对数不当作独立样本量。真机成功率约 0.5 时 n=20 的 95% 半宽约 0.22，n=100 约 0.10，正式报告用 Wilson 区间。

预注册目标：覆盖率 70% 时相对最强适用基线降低排序错误率约 20%，或同错误率下少用 30% 真实证据。这是推进指标，不是必达结果。

## 13. 预算、进度与停止条件

**计算。** 单张 24 GB GPU、64 GB RAM 起步；策略推理与仿真分阶段运行。总运行量 = 参数设计点 × 策略 × 场景 × rollout × 校准预算 × 重复，第 3 周用实测吞吐填表。响应面代理下，每任务 64–256 个设计点为原型规模。

| 阶段 | 时间 | 交付与继续条件 |
|---|---|---|
| 通道核验与预注册 | 第 1 月 | 三通道核验表；SIMPLER 一任务跑通；图 1 初版；预注册冻结 |
| 机制验证 | 第 2–3 月 | 实验 A 完成；冻结随机情境集；激励失配判据的合成验证 |
| 核心方法与图谱 | 第 4–5 月 | 集合、差值传播、拒判、证据选择；实验 B 图谱；关键消融 |
| 真机验证 | 第 6–7 月 | 实验 C（SIMPLER 历史标签）；AutoEval 探测作业提交与回收 |
| 第二平台与失配 | 第 8–9 月 | RoboArena 提交（若可行）；实验 E |
| 写作与复现包 | 第 10–12 月 | 歧义案例集、检查点哈希、完整命令；首次投稿 |

**必做项：** A、B、C、F，以及 D 的离线赛道。**条件项：** D 的真实赛道（依赖 AutoEval）、RoboArena 第二平台、PolaRiS。

**停止／转向条件。** 首月无 A 级赛道则收缩为“真实轨迹约束下的动力学歧义分析”；第三月图谱翻转比例低于 5% 且与激励失配无关则不再堆方法；第五月收益仅来自拒判或额外预算则重做公平比较；只在一个平台／一个策略家族有效则降低泛化主张。六个月投稿版本：SIMPLER 单赛道 + 实验 A、B、C、F。

## 14. 关键图表

- 图 1：同一真实轨迹、两个拟合误差相同的物理解释、两条预测帧序列、两个策略在两种解释下的成功率交叉。
- 图 2：证据 → 相容集合 → 差值传播 → 部分序 → 证据选择的流程，标出 α、β、δ 三个用户参数。
- 图 3：拟合误差 vs. 排序错误（散点，弱相关）与集合宽度 vs. 排序错误（强相关）并排。
- 图 4：激励失配指标 vs. 翻转发生率（图谱结果）。
- 图 5：公平拒判下的风险—覆盖率曲线。
- 图 6：证据预算曲线，离线赛道与 AutoEval 真实赛道。
- 图 7：真机验证：可判定结论一致率、拒判对的真机区间、结构失配报警案例。

主表：赛道与独立样本量、基线结果、消融、真机标签不确定性、运行成本。附录：参数范围、ε 校准程序、优化细节、检查点哈希、数据权限、失败案例、完整命令。

## 15. 投稿定位

按内容与审稿人池匹配排序，所有刊物的中科院分区须按本单位年度目录经[官方入口](https://www.fenqubiao.com/)核验，本文不把任何刊物标为“已确认 TOP”。

| 候选 | 定位 | 适配条件 | 发表路径 |
|---|---|---|---|
| IEEE Transactions on Robotics | **主投** | 机器人方法创新 + 机制解释 + 多平台真机证据；AutoEval／RoboArena 结果明显加分 | [作者指南](https://www.ieee-ras.org/publications/t-ro/t-ro-information-for-authors/)：Hybrid，有传统路径，超页费另计 |
| International Journal of Robotics Research | 备选 | 理论与实证均充分、形成普适结论 | [作者指南](https://journals.sagepub.com/author-instructions/ijr)：Subscription，OA 可选 |
| Science Robotics | 冲刺，仅当真机证据充分 | 需要跨平台真机验证与清晰的科学命题；篇幅与写法完全不同 | 订阅制期刊；费用与单位目录需核 |
| IEEE Transactions on Automation Science and Engineering | 自动化视角备选 | 强调减少真实测试成本 | [作者指南](https://www.ieee-ras.org/publications/t-ase/information-for-authors-t-ase/)：Hybrid |
| RA-L | 紧凑版本 | 若收缩为六个月版本 | Hybrid；TOP 状态需核 |

初版方案将 Information Sciences 与 Neural Networks 列为主投候选，本版移除：二者的审稿人池不评估机器人 Sim2Real 证据，论文的真机验证在那里不加分，且影响力低于机器人旗舰刊。仅在方法被定性为纯统计决策方法且机器人证据薄弱时再考虑。

## 16. 前四周清单

第 1 周：冻结文献版本，补读 3.2 节六类理论近邻与 Real-is-Sim、AutoEval、RoboArena；核验 SIMPLER 检查点与 REAL_PERF；核验 AutoEval 队列状态；抽查 RoboArena 快照元数据；写预注册草稿。

第 2 周：跑通 SIMPLER 一个任务与一个公开策略；确认动作单位与控制频率；从 BridgeData V2 或 fractal 取出匹配子集；记录吞吐。

第 3 周：单点校准与多模式集合搜索；在推物任务中生成图 1 初版；实现共同随机数与同时置信带；填计算预算表。

第 4 周：第一张公平拒判的风险—覆盖率曲线；三通道核验表；预注册冻结；决定进入主方案还是收缩方案。

第一项最有价值的成果不是训练模型，而是确认：**在可复现实验中，确实存在“拟合得同样好、却选出相反策略”的问题，它的发生可以用激励失配解释，并且能在公开真机通道上得到独立检验。**

## 17. 数据使用与伦理

DROID 为 CC-BY 4.0；RoboArena 快照与 AutoEval 视频含评测者与环境，仅用于研究分析，不再分发原始视频；发布的歧义案例集只含轨迹摘要、参数集合与检查点哈希。AutoEval 与 RoboArena 的使用遵守平台规则，提交前确认任务与动作授权。
