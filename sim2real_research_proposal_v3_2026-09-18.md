# 决策相关可辨识性：校准仿真何时能可靠比较机器人策略

研究方案 v3｜2026-09-18｜执行基线与审阅稿

本版承接 v2 与审计报告，修正科学命题、统计保证、平台状态与计算预算。v1/v2 保留为历史记录。本版将方法设计、当前可执行原型和待满足的数据条件分开陈述；原型运行结果以执行脚本产生的文件为准，不在方案中提前写入预期结论。

**现有条件：** 无自有机器人；Windows 主机；RTX 5060 Laptop，约 8 GB 显存。WSL SAPIEN 2.2.2 CPU engine 可启动，但渲染失败。Windows SAPIEN 3／ManiSkill 3 已通过四个 WidowX 场景的图像和动作检查；使用改变 IK 行为的进程内兼容适配，不能等同于历史主分支复现。已读取一条 38 步 Bridge 真实示范，未做参数校准。详见 [执行记录](docs/execution_status_2026-09-18.md)。

**投稿定位：** 按 IEEE T-RO／IJRR 的机器人方法与证据要求设计；选择允许传统订阅发表的路径。当前未逐刊核实用户单位采用年度的中科院大类、小类和 TOP 字段，不承诺分区、录用或免费发表。

## 1. 研究要回答什么

使用真实轨迹校准仿真，再据此挑选机器人策略时，校准数据可能无法区分会改变策略优劣的模型。我们研究：**校准数据提供的哪些信息足以支持策略比较，哪些额外证据能最有效地消除与选择策略有关的歧义？**

核心科学主张改为：

> 完整参数辨识不是策略差值辨识的必要条件；有限数据下的整体轨迹拟合精度，也不是排序可靠性的充分指标。决定因素是未确定方向对策略差值的影响，以及策略差值相对于实践阈值的间隔。

这不是“参数可辨识性与排序可判定性互不蕴含”。在模型正确、完整联合参数唯一可辨识且策略价值定义固定时，价值泛函也可辨识。即便差值完全已知，若真实优势不超过实践阈值，也不应宣布显著优胜。

本研究区分三个对象：

1. **结构可辨识性：** 无限同类型观测能否区分参数／策略差值。
2. **有限样本证据强度：** 当前数据给出的不确定区间有多宽。
3. **实践优势：** 真实差值是否超过事先确定的阈值 δ。

## 2. 论文拟形成的贡献

| 拟贡献 | 需要交付的证据 | 当前阶段 |
|---|---|---|
| C1：校准激励与策略敏感方向的失配机制 | 明确反例、定量局部判据、预先冻结情境中的规律 | 从精确线性原型开始 |
| C2：不确定性传播与有条件的策略比较 | 有效参数检验反演、连续极值或可核算近似、同时正确性的前提 | 原型可精确实现；机器人部分待完成 |
| C3：面向策略可判定性的证据选择 | 相同可见信息与预算下，优于经典定向实验设计等强基线 | 首期实现经典 c-optimal/D-optimal 对照；新效用待开发 |
| C4：机器人策略歧义图谱与真实外部验证 | 可复现策略、任务和参数审计；冻结测试；历史真实标签验证 | SIMPLER 为主线，条件满足后扩展 |

C1–C4 是拟检验的贡献，而不是已证明的创新声明。集合辨识、控制导向辨识、部分辨识、c-optimal 设计、置信排序和拒判本身均有既有理论。论文能否成立，取决于机器人机制、可计算方法和外部证据是否形成新增知识。

不将普通 VLM 事件提取增加为主线模块。“基础模型”体现为被评估的冻结 VLA 策略。首期线性原型只有两个解析控制通道，尚不能支持任何关于 VLA 或真实机器人的实证结论。

## 3. 理论与算法定义

详细数学协议见 [docs/theory_protocol.md](docs/theory_protocol.md)。本节给出实现必须遵循的核心定义。

令联合未知量 z=(θ,ν,κ)，分别对应动力学、观测标定和控制接口参数；D 为真实校准证据。固定任务上下文及初始状态分布，对同一 z 比较策略：

\[
\Delta_{ij}(z)=J_{\mathrm{sim}}(\pi_i;z)-J_{\mathrm{sim}}(\pi_j;z).
\]

### 3.1 通过有效候选检验反演构造参数集合

对每个候选 z，明确观测概率模型，并构造在该候选为真时有效的检验 p_z(D)：

\[
P_z\{p_z(D)\le\alpha\}\le\alpha,
\qquad \mathcal C_\alpha(D)=\{z:p_z(D)>\alpha\}.
\]

因此真实参数在模型内时，有 P(z*∈Cα)≥1−α。这一声明针对固定的采样和分析程序。

普通 split conformal 的新观测预测覆盖不能直接替代参数置信集覆盖。轮廓似然的 χ² 阈值通常依赖正则性与渐近近似；“留一轨迹残差分位数”也不是似然比统计量临界值。非线性机器人模型需完整说明模拟检验校准或其他有效构造；经验残差阈值只能称经验相容集合。

所有策略对可共享同一参数覆盖事件，参数层无需按策略对数再修正。但仿真采样、代理误差、结构偏差及多次查看结果均须独立处理，不能由该事实一并消除。

### 3.2 局部椭球与正确的激励失配判据

若局部参数集合为 (z−ẑ)ᵀI_D(z−ẑ)≤q²，且差值可线性化为 Δ̂+gᵀ(z−ẑ)，在 I_D 正定、未受额外参数边界截断时，其半宽为

\[
h=q\sqrt{g^\top I_D^{-1}g}.
\]

若仿真、代理及结构偏差的总半宽为 η，则实践优势的充分判据是

\[
|\widehat\Delta_{ij}|>\delta+h+\eta.
\]

方向以 Δ̂ 的符号确定。gᵀI_D⁻¹g 的阈值取决于差值余量 |Δ̂|−δ−η，而不是单独由 δ 决定。

小但正的信息特征值可以随同分布样本增加而积累；真正未激励的线性方向，或一般非线性模型中的观测等价类，才可能造成永久歧义。存在零空间投影也不必然不可排序：物理参数有界、优势足够大时仍可判定。不得将“弱信息”“局部零导数”“全局不可辨识”混用。

### 3.3 从相容参数到策略结论

对全部参数与策略对构造同时有效的仿真差值带 [Δ⁻,Δ⁺]，定义

\[
L_{ij}=\inf_{z\in\mathcal C_\alpha}\Delta^-_{ij}(z),\qquad
U_{ij}=\sup_{z\in\mathcal C_\alpha}\Delta^+_{ij}(z).
\]

若在一个共同事件上，真实差值与所用模拟值的结构偏差由 b 控制，则 [L−b,U+b] 可用于决策：下端大于 δ，宣告 i 有实践优势；上端小于 −δ，宣告 j 有实践优势；其余输出证据不足。

空集合不采用空集 inf/sup 的数学惯例给出胜负，必须返回“参数／模型／数值程序不相容”。非空但整个区间位于 [−δ,δ] 时，可以额外标注“当前证据支持实践等价”；仅跨阈值的拒判不等于等价。

有限参数样本的极值、未经认证的 GP 后验带均不自动保证覆盖连续参数空间。首期解析原型做连续极值；SIMPLER 先报告经验范围，再根据计算预算增加可验证误差控制。

### 3.4 结构偏差与保证范围

对称偏差校正使用绝对残差 |r|，或分别控制上下尾；有符号残差的一个上分位数不能用于对称扩展。全策略对的共同保证需要校准场景／session 块内最大残差等共同事件，单对边际覆盖不够。

真实成功率的抽样误差必须进入偏差估计。真机聚合表仅有均值而无试验次数时，不构造虚假的二项试验或逐次样本。

操作上区分两种路径：

- **条件定理路径：** 明确参数覆盖、全参数仿真带、全策略对偏差覆盖三个事件及其假设，才由 union bound 得出同时正确性的结论。
- **独立区间校准路径：** 固定完整评估流水线，对独立场景块校准其输出区间到真实差值的距离；在交换性等条件下得到新场景块的边际覆盖。它不是物理参数置信集，也不自动适用于分布漂移后的任务。

SIMPLER 只有少量独立任务，通常不足以提供强而稳定的结构偏差校准。届时报告外部覆盖诊断，不制造保证。

### 3.5 同时正确性与选择性风险

若某次完整实验中“任何已宣布结论错误”的概率≤η_total，这是 family-wise 的事件保证。它不直接等于跨场景汇总后“已宣布结论中的错误比例≤η_total”。

论文分别报告：参数覆盖率、区间覆盖率、任何错误的实验比例、给出结论的比例，以及已判定结论中的错误率。最后一项需要独立统计分析。α 是统计检验水平，δ 是实践差异阈值，两者不可通过测试结果事后互换调优。

## 4. 可执行原型：首期不依赖机器人或大显存

与实现代理协调的数学定义如下，具体执行状态由结果文件记录。

\[
\theta\in[0,1]^2,\quad y_t=x_t^\top\theta+\epsilon_t,\quad
\epsilon_t\overset{iid}{\sim}N(0,0.15^2).
\]

两个冻结通道 π₁=e₁、π₂=e₂ 的期望进度为 J₁=θ₁、J₂=θ₂，故 Δ=θ₁−θ₂，δ=0.05。这是控制增益校准的抽象代理，不声称具备接触物理或视觉闭环。

首批校准观测包含 8 个设计点。定义 s=(1,1)/√2、d=(1,−1)/√2：

- null：全部沿 s，无法辨识 d 方向；
- weak：沿归一化的 s±0.03d；
- balanced：平衡覆盖 s 和 d。

已知噪声、固定设计下，用 rank(X) 自由度的投影 χ² 检验反演：

\[
\mathcal C_\alpha=
\left\{\theta\in[0,1]^2:
(\theta-\widehat\theta)^\top X^\top X(\theta-\widehat\theta)
\le \sigma^2\chi^2_{r,1-\alpha}\right\}.
\]

rank=1 时这是条带与方框相交；rank=2 时为椭圆与方框相交。对线性 Δ，在椭圆驻点、方框边交点和可行顶点中求连续极值，无需将离散网格当认证界。

离线证据选择候选 x∈{s,d,e₁,e₂}，每方向 16 条独立记录组成 64 条有限池，比较 random、logdet D-optimal、正则 c-optimal。补充预算为 0、2、4、8、16、32；300 个 θ∼U([0.1,0.9]²) 的重复情境。主分析 α=0.05，另报告预先给定的 α 曲线。选择器只使用设计向量，不预读响应，不根据 y 调整，因此各预算截面的固定设计检验有效。机制曲线另固定 θ=(0.6,0.4)，在 n=8 至 8192 的预设样本量下各重复 300 次。

这些是探索性机制实验。不同预算的结果不能解释为可选停止下的同时保证。后续若依据响应选择日志，需使用可预测设计的有效检验、模拟完整选择程序、独立确认集或置信序列。

**原型验收内容：** 已知噪声下参数与差值覆盖是否接近名义水平；null 与 weak 能否区分；强真实差值也会被拒判；在同预算下定向信息是否更快缩短差值区间。c-optimal 是经典基线，不能将其结果包装为 C3 已完成。

### 4.1 已生成的探索性结果

首轮输出见 [pilot/outputs/summary.json](pilot/outputs/summary.json)，配置见 [pilot/config.json](pilot/config.json)。以下仅解释当前 300 次重复的结果：

- n=8 增至 8192 时，null 的差值平均区间宽度维持约 2；weak 从约 1.97 降至 0.383；balanced 从约 0.519 降至 0.0162。该结果符合 true null 与 weak 的区别。
- 补充预算 8 时，D-optimal 与 c-optimal 的选择相同，集合方法结论覆盖率均为 51.7%，random 为 37.7%。预算 16 时约为 59.0%、60.7% 和 50.3%。没有证据据此声称 c-optimal 显著优于 D-optimal。
- 单策略对的经典 scalar Wald 区间在 D-optimal、预算 8 下结论覆盖率为 58.7%，高于共享参数集合的 51.7%。共享集合并非单对最有效的办法，必须保留该强基线。

这些结果支持机制与实现检查，不证明新算法贡献、机器人现象普遍性或真实迁移效果。增加样本与任务必须依据预先提出的新问题，不能为了得到有利显著性而不断重跑。

## 5. 研究问题与可证伪假设

| 问题 | 检验方式 | 不支持时的处理 |
|---|---|---|
| RQ0：歧义在机器人评估中有多常见？ | 对预注册任务与可重跑策略组合统计区间跨零、跨±δ和宽度 | 若稀少且影响轻微，报告边界并停止堆叠方法 |
| RQ1：激励失配能解释哪些排名错误？ | 受控 true null/weak/full；真实参数范围和敏感方向分析 | 若关联仅由范围选择造成，重审机制 |
| RQ2：保留歧义是否改善可靠性？ | 同覆盖率风险、同风险覆盖率、选型遗憾及成本 | 若收益全来自过度拒判或额外预算，则不成立 |
| RQ3：针对决策选证据是否优于经典设计？ | 与 c-optimal、D-optimal、参数熵和随机比较 | 若无法超越经典方法，以机制与验证为主、缩减算法创新 |
| RQ4：仿真结论能否预测历史真实策略差异？ | 冻结算法后，用匹配的 SIMPLER 历史标签作外部检验 | 若匹配或真实样本量不足，明确减弱外部主张 |

RQ4 不再把“拒判对真机差值小”设为必要预测。拒判可能恰恰来自尚未观测的关键方向，即使真实优势很大也应拒判。可研究拒判原因与真实余量的分布，但不能用真实平局比例作为拒判机制正确性的定义。

不预设“必须翻转 15%”等数据结论。首轮探索用于估计量级和独立样本量，随后另行冻结验证集与主要假设。

## 6. 数据、平台与已经知道的边界

以下状态继承用户 2026-09-18 的核验及项目审阅。当前执行若发现差异，以带时间、版本和实际返回信息的核验记录更新。

| 来源 | 已知事实与用途 | 当前限制 |
|---|---|---|
| [SIMPLER](https://github.com/simpler-env/SimplerEnv) | 公开仿真、控制器辨识流程、若干策略检查点、REAL_PERF 历史真机成绩；主验证通道 | 平台生态一致不自动意味着场景、物体、session 完全匹配 |
| RT-1／RT-1-X／Octo | 用户已核验公开检查点地址可访问 | 可访问不等于本机可推理；版本、权重与历史表键需逐个匹配 |
| RT-2-X | REAL_PERF 中有历史标签 | 无公开检查点，不纳入需重跑的策略集合 |
| BridgeData V2／fractal | SIMPLER 控制器辨识使用的真实轨迹来源 | 现成流程主要约束机器人状态和控制响应，不证明可辨识物体摩擦、质量 |
| [AutoEval](https://auto-eval.github.io/) | 有历史远程真机平台与日志 | 当前公开端点离线；2026 年开放计划未确认；仅条件项 |
| [RoboArena](https://arxiv.org/abs/2506.18123) | 当前官网、策略表单及远程服务要求已只读核验；见研究审计记录 | 额度、排队时间、申请获准及指定探测权限未确认；DROID 与 SIMPLER 具身不同 |
| DROID／PolaRiS | 潜在第二生态的数据和仿真入口 | 权重版本、场景与动作协议须独立匹配，现不作为主线依赖 |

AutoEval 日志或代码近期更新只说明维护／数据更新迹象，不能推断当时远程真机队列在线。当前执行不发送邮件、不提交策略、不创建常驻服务或开机启动项。

## 7. SIMPLER 主线实施

### 7.1 先做控制响应歧义

首期物理参数优先级：控制增益、阻尼、有效时延。先使用公开 sysid 轨迹与末端状态，核对动作单位、控制器类型、频率和相机／机器人坐标。

物体摩擦、质量和接触参数只有在具备匹配物体轨迹、可重复场景以及足够激励时才加入。控制误差拟合出的“有效参数”不解释成真实材质参数。

初期最多 2–3 个未知参数；待样本、辨识性与计算量支持后再扩到 4–6 维。先固定观测标定时，结果必须写明条件于该标定。

### 7.2 8 GB 显存路线

1. CPU 完成线性原型、统计程序、数据字段审计和历史成绩分析。
2. 核实 Windows/WSL/Linux、CUDA、渲染与官方依赖的兼容性；不擅自安装系统组件或配置自动启动。
3. 选择最小、版本可匹配的冻结公开策略做单 episode 推理；batch=1，记录峰值显存、启动内存、每步延迟与仿真帧率。
4. 策略推理与仿真分进程串行／分阶段运行；先少量参数点，及时保存轨迹与摘要。不能把相同图像的缓存误作不同物理条件下的闭环重跑。
5. 若 8 GB 无法运行匹配策略，先保留 CPU 机制与公开日志工作。量化或改权重必须视作新策略，不沿用原策略的历史真机标签。扩大算力作为后续资源选项，不伪报本机成功。

目标首跑为 1 任务、1 可运行策略；正式排名至少需要 2 个匹配策略，有条件时扩至 ≥3 个。预计任务全量图谱的可行规模由实测吞吐决定，不预承诺全基准。

### 7.3 历史真机标签协议

为每个策略—任务记录：权重 URL/哈希、代码 commit、预处理、动作空间、控制频率、初始条件、仿真版本、历史真机成功数／试验数及出处。

已浏览过的 REAL_PERF 全表不能再声称对研究者完全盲测。可采取代码冻结后由脚本一次性评估、保留任务／策略组合，或新增真实外部评测；如实说明历史公开标签已经可见。

历史成绩允许外部一致性检验，不代表执行了新真机实验。聚合标签在已知试验次数及匹配协议下可形成成对区间；未知试验次数则只做带限制的描述分析。

## 8. 基线与公平比较

| 类别 | 首期对应物 | 机器人阶段要求 |
|---|---|---|
| 单点拟合 | 最小二乘点估计并报告完整排序 | SysID + 轨迹级 bootstrap；说明模型假设 |
| 参数分布 | 可选高斯／后验敏感性对照 | DROPO/BayesSim 类适用实现；后验分位数不冒充频率学保证 |
| 参数集合 | 精确 χ² 反演 + 连续极值 | 有效／近似校准程序分开报告 |
| 直接差值区间 | 单个已指定策略对的精确 scalar Wald 区间 | 可用时保留直接功能量推断；多对比较单独控制 |
| 证据选择 | random、D-optimal、c-optimal | 加参数熵、误差优先、提出的决策效用 |
| 外部效果预测 | 原型不需要 | 监督条件匹配时纳入 Sim2Val、SCAPE 等 |

各基线都报告完整曲线；同 δ、相同证据预算、计算预算与测试场景。并非所有方法填入相同的 α 数值就有相同的统计覆盖，需在独立开发数据上校准其拒判阈值，再到冻结测试集比较。默认仿真仅控制 rollout 噪声，不因此覆盖模型误差。

使用历史真实结果训练的方法与仅用校准轨迹的方法分开列监督条件。相同策略对共享策略、任务和 episode，不把对数当独立样本量。

## 9. 实验矩阵与统计指标

| 实验 | 内容 | 证据等级 |
|---|---|---|
| A0 | 二维解析原型：null/weak/balanced、连续置信集极值 | 精确模型内机制 |
| A1 | 有界控制代理与固定任务策略；增益/阻尼/时延扩展 | 合成机制 |
| B | SIMPLER 参数范围、匹配轨迹与策略歧义图谱 | 机器人仿真与真实校准轨迹 |
| C | 冻结方法对照 SIMPLER 历史真实成绩 | 历史真机外部验证 |
| D | 离线候选日志证据选择 | 数据获取／标注／计算预算节省，按实际场景命名 |
| E | 模型结构失配、未见参数和任务、代理误差 | 稳健性与边界 |
| F | AutoEval 恢复或 RoboArena 审核通过后的新增评测 | 条件性的前瞻真机验证 |

主指标：实际参数覆盖率；差值区间覆盖率；固定覆盖率下的错误排序率；固定错误率下的结论覆盖率；实践优势误宣率；选型遗憾；真实证据和计算成本。

其中“实践优势误宣”包括方向正确但真实优势≤δ；另报纯符号错误。拒判不计作正确结论，也不默认计作错误。零个已判定样本时，选择性错误率记为不可定义，不写成 0。

独立重复的统计单位是情境／数据集；真实任务使用任务／session 聚类。只在任务很少时不依赖不稳定的 cluster bootstrap 制造精确区间，应展示逐任务结果和敏感性。

真机真值本身有区间时，分类为支持、矛盾和真实未决，报告全部分母；不能简单剔除困难对后宣称高正确率。按计划重复次数给覆盖率的二项置信区间。分布漂移与结构失配单独报告，不能混入已知模型覆盖定理。

## 10. 证据选择：从经典基线到拟议方法

第一阶段的 c-optimal 目标为减少 gᵀI⁻¹g，适合固定线性差值。这一部分服务于机制验证与强基线复现，不是新增算法。

拟议方法的目标是：在多任务、多策略对、非线性差值下，预测候选证据 e 被揭示后，实践可判定策略对数或部署遗憾的改善，除以获取成本。必须包括：

- 与 c-optimal、D-optimal、参数熵和随机的公平比较；
- 多个策略对、不同实践余量、不同获取成本；
- 选择器只能看到尚未揭示的元数据和当前已观察数据；
- 模拟未来响应的计算费用计入预算；
- 偏好某个参数方向但未改善实际选择时，记录失败原因。

若只有一对线性策略且噪声已知，新效用可能退化为经典 c-optimal；不将这一特例的改名作为贡献。

## 11. 阶段安排和当前自动执行范围

| 阶段 | 交付 | 继续门槛 |
|---|---|---|
| 当前执行 | v3、数学协议、环境与资源清单、可运行解析原型及结果 | 程序有效、结果与数学一致 |
| 第 1–2 周 | 官方仓库版本、策略—真实标签映射、最小依赖与吞吐核验 | 至少一个可复现任务和一个可运行策略 |
| 第 3–4 周 | 至少两策略、低维控制校准、预注册草稿与首张歧义图 | 匹配数据、真实标签来源、预算成立 |
| 第 2–3 月 | 受控机制扩展与冻结情境；经典强基线 | 现象不依赖精选案例 |
| 第 4–5 月 | 非线性/多策略证据选择、经验图谱、主要消融 | 收益超过公平强基线或调整贡献 |
| 第 6–7 月 | SIMPLER 历史真机验证；可用时新增真机 | 真实证据范围与主张相符 |
| 第 8–9 月 | 条件性第二平台、失配与复现包 | 不以平台不可用拖延主线分析 |
| 第 10–12 月 | 写作、期刊规则与单位分区核验、首次投稿 | 贡献和证据达到目标刊要求 |

当前“自动执行”指推进本地研究工作、读取公开资料和运行可逆实验，不等于已经启动数月后台作业。持续调度若另行设置，需遵守用户对自动启动的明确限制。任何官方通信、策略提交、付费算力或对外发布都不由这份方案自动完成。

停止／转向条件：8 GB 路线无法运行匹配策略时调整计算部署；三至四周仍不能匹配历史标签与策略时收缩外部主张；歧义仅来自不合理参数边界时修正设计；改进只来自低覆盖率或额外预算时不宣称方法优势；真实证据不足时不声称已经证明 Sim2Real 部署能力。

## 12. 写作与投稿证据门槛

主投定位仍是 T-RO，IJRR 为适配候选；投稿顺序在实证完成后确定。自动化成本贡献突出时再评估 T-ASE。只有形成跨平台、有说服力的新增真实证据和明显科学发现后，才讨论更高冲刺目标。

所需主图：同一证据对应相反策略选择的可复现例子；方向性信息与差值余量；公平风险—覆盖率；离线证据预算；历史真机外部验证；计算成本和失败边界。

本地预注册至少冻结：θ/z 范围、观测模型、α/δ、任务和策略版本、证据选择可见字段、主要指标、重复次数、数据拆分、失配处理、真实标签未决处理。正式公开注册或投稿是单独动作。

非 OA 目标按投稿当年的官方作者指南确认 Traditional／Subscription 选项，同时核对超页费、数据与代码政策。中科院分区和 TOP 以用户单位采用版本为准，不用 JCR Q1 代替。

## 13. 文献审阅和来源使用

需逐篇确认差异：集合成员辨识、控制导向辨识、值等价、决策导向学习、置信离线策略选择、部分辨识与 c-optimal 实验设计；机器人近邻包括 DROPO、ASID、SIMPLER、Real-is-Sim、Sim2Val、AutoEval、RoboArena、PolaRiS，以及用户已核验存在的 SCAPE 和 R2S-Eval。

旧稿的“无拒判／无选择”等二元比较不能仅凭摘要保留，必须在论文正文或代码中核对。新颖性表只记录明确证据、版本和页码。公开数据与代码许可逐仓库检查，不把一个数据集的许可外推到视频、检查点或衍生包。

本版不增加未经核验的论文结果或平台可用性结论。参考入口：

- SIMPLER：[论文](https://arxiv.org/abs/2405.05941)、[代码](https://github.com/simpler-env/SimplerEnv)。
- AutoEval：[官网](https://auto-eval.github.io/)、[论文](https://arxiv.org/abs/2503.24278)。
- RoboArena：[论文](https://arxiv.org/abs/2506.18123)、[历史快照](https://huggingface.co/datasets/RoboArena/DataDump_07-17-2026)。
- [DROPO](https://arxiv.org/abs/2201.08434)、[ASID](https://arxiv.org/abs/2404.12308)、[Real-is-Sim](https://arxiv.org/abs/2504.03597)。
- [SCAPE](https://arxiv.org/abs/2608.19425)、[R2S-Eval](https://arxiv.org/abs/2609.03276)。
- [T-RO 作者指南](https://www.ieee-ras.org/publications/t-ro/t-ro-information-for-authors/)、[IJRR 作者指南](https://journals.sagepub.com/author-instructions/ijr)、[中科院分区官方入口](https://www.fenqubiao.com/)。

## 14. 相对 v2 的关键修订

1. 纠正“互不蕴含”，将结构辨识、样本不确定性和实践间隔分开。
2. 参数集合改为有效检验反演，去掉普通 conformal 预测覆盖到参数覆盖的跳跃。
3. 局部判据加入真实差值余量与其他误差，区分弱信息和零信息。
4. 偏差校准改为绝对／双尾以及块内共同事件，明确真实标签噪声。
5. 区分同时事件保证和选择性错误率；拒判不再被解释为真实平局。
6. AutoEval 全文统一为离线条件项；RoboArena 公开提交入口已核验，额度、排队和受控任务条件仍待确认，详见 [提交审计](research_audit/roboarena_submission_audit_2026-09-18.md)。
7. SIMPLER 主线从“完整物体物理闭环”收缩到先复现控制响应校准；RT-2-X 不重跑。
8. 24 GB 默认预算调整为约 8 GB 实际资源，先做 CPU 解析实验和最小冻结策略推理。
9. 将已实现的经典实验设计和拟议的新增决策效用分开。
10. 公开历史成绩不再宣称完全盲测；分区与期刊可行性不作未经核验的承诺。
