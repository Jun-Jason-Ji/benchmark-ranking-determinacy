# 研究方案审计报告：《面向机器人基础模型的物理歧义感知仿真校准与可信 Sim2Real 评估》

审计日期：2026-09-18。审计对象：`sim2real_research_proposal_2026-09-18.md`。审计目标：以中科院 1 区 TOP／机器人领域旗舰期刊（T-RO、IJRR 级）审稿人的标准，判断该方案的研究价值是否足够，并列出必须修复的问题。修订后的方案见 `sim2real_research_proposal_v2_2026-09-18.md`。

## 总体判断

原方案的长处是诚实、风险意识强、基线和统计陷阱列得很全。短板正好是它的反面：**全文防御性陈述远多于贡献性陈述，读者读完不知道这篇论文到底证明了什么新东西。** 以 TOP 期刊标准衡量，当前版本的科学价值主张不够锋利，技术核心（在可行参数集合上取策略差值的上下界）容易被审稿人归为“集合成员辨识 + 部分辨识的直接套用”，真实验证路径又被“没有自有机器人”这一自设约束压得过低。

结论：**方向有价值，但按当前写法投 TOP 期刊大概率被要求“重新定义贡献”或直接拒稿。** 需要修复的不是细节，而是四件结构性的事：
1. 换一个正面的科学命题作为全文主线（决策相关可辨识性）。
2. 补齐五类被漏掉的近邻文献，并据此重新划定技术贡献。
3. 把三条零硬件真机通道（SIMPLER 自带真机结果 + 公开检查点、AutoEval 公开真机队列、RoboArena 策略提交）从“增强实验”提升为主验证。
4. 给可行集合阈值 ε 和仿真结构偏差 b 以统计语义和估计方法，否则风险—覆盖率曲线无法解释。

以下按严重程度分级。P0 = 不修则不具备 TOP 期刊竞争力；P1 = 显著影响评分；P2 = 细节。

## P0：结构性问题

### P0-1 没有正面的科学命题和贡献清单

原方案第 1 节把“中心问题”写成一句复合疑问，第 2 节的表格全部是“本项目不能重复声称的创新”，第 6 节理论部分开头是“不能声称理论概念首创”。审稿人看完的印象是“作者知道自己不新”，而不是“作者发现了什么”。

**修复：** 把全文改写为围绕一个可命名的概念：**决策相关可辨识性（decision-relevant identifiability）**。定义：给定真实证据 D 与实践差异阈值 δ，策略对 (i,j) 是 δ-可判定的，当且仅当在所有与 D 相容的物理解释上，策略差值 Δij 的符号一致且幅度超过 δ。核心命题：**参数可辨识性与决策可辨识性互不蕴含**。θ 不可辨识时 Δ 可能可判定（差值对歧义方向不敏感），θ 拟合很好时 Δ 也可能不可判定（校准数据的激励方向与目标策略的敏感方向正交）。然后给出四条贡献：概念与理论、可计算证书、面向可判定性的证据选择、含真机验证的经验图谱。

### P0-2 漏掉五类近邻文献，导致技术贡献边界划错

原方案的近邻表只覆盖机器人 Sim2Real 与两篇离线策略评估。以下五类文献任何一个懂行的审稿人都会提，且直接决定“什么算新”：

| 类别 | 代表工作 | 与本方案的关系 | 必须做的区分 |
|---|---|---|---|
| 集合成员辨识 | Milanese & Vicino, *Automatica* 1991；Fogel & Huang 1982 | 原方案 5.2 的 Zε 就是集合成员估计的可行参数集 | 本方案的新意不在集合本身，而在把集合传播到策略差值符号并据此拒判 |
| 面向控制的辨识（I4C） | Gevers 1993/2005 综述；Van den Hof & Schrama 1995 | “只辨识对控制器重要的部分”是三十年前的控制论命题 | 本方案是 I4C 在“闭环视觉策略集合比较”上的对应物：控制器不可解析、只能 rollout |
| 值等价原理 | Grimm et al., NeurIPS 2020；Proper Value Equivalence 2021 | 模型只需对给定策略集在值上等价 | 本方案研究的是“数据无法区分的值不等价模型”，是值等价的对偶问题 |
| 决策导向学习 | Elmachtoub & Grigas, *Management Science* 2022 | 以下游决策损失训练预测器 | 本方案不训练预测器，而是判定决策是否被数据支撑 |
| 离线策略选择的置信方法 | Kuzborskij et al., AISTATS 2021；Paine et al. 2020 | 用置信下界选策略 | 这些方法要求行为策略概率与状态覆盖；本方案在物理参数空间而非重要性权重空间做部分辨识 |

另外三篇机器人近邻也缺：**Real-is-Sim**（Abou-Chakra et al. 2025，动态数字孪生评估行为克隆策略）、**AutoEval**（Zhou et al., CoRL 2025，公开的真机自动评测）、**RoboArena 论文本身**（Atreya et al. 2025，分布式双盲成对真机评测）。前者是竞争路线，后两者是本方案可以直接使用的真机验证通道。

**修复：** 在近邻表中补齐；把 2 节的表格从“不能声称”改写为多维差异表（歧义表示：点／分布／集合；辨识目标：θ／Δ 符号；是否拒判；是否做证据选择；是否有真机成对验证）。

### P0-3 严重低估了零硬件条件下能拿到的真机证据

原方案默认“没有自有机器人，则真实闭环策略排名无法作为主结论”，把真机验证放到第 6–7 月且高度依赖数据审计结果。实际上有三条公开通道，方案要么没提，要么只当成数据包用：

1. **SIMPLER 已经是一条完整的 A 级流水线。** 它自带 Google Robot 与 WidowX 两套仿真环境、RT-1 系列／RT-1-X／Octo 等策略的真机成功率表（论文表格与仓库中的 REAL_PERF），并提供 RT-1 系列检查点下载链接，Octo 检查点公开。真实校准轨迹也有匹配来源：WidowX 对应 BridgeData V2，Google Robot 对应 Open X-Embodiment 中的 fractal 数据。原方案第 7 节把它说成“聚合成功率不是逐次日志”，但对于**策略排名**这一目标，聚合成功率加已知试验次数已经是足够的真实标签。
2. **AutoEval 提供公开的 WidowX 真机评测队列。** 用户像提交集群作业一样提交策略，系统自动判成功、自动重置，官方称结果与人工评测高度一致，涵盖水槽抓放、抽屉、布料等五个任务。这意味着“真实主动探测”与“新策略的真机验证”在零硬件条件下是可做的，只受排队与可用性限制。
3. **RoboArena 评测网络已对社区开放策略提交**，在 DROID 平台（Franka + Robotiq）上做双盲成对评测。DROID 数据集又提供同一平台的真实轨迹，PolaRiS 重建的也是 DROID 风格场景。三者构成同一机器人生态，原方案把它们当成三个孤立来源。

**修复：** 把“增强实验 F”升级为主实验之一；以 SIMPLER 生态作为第一条完整闭环，以 AutoEval 作为真实探测通道，以 RoboArena 提交作为第二平台外部验证。首月门槛相应改为“核验三条通道的可用性与匹配性”，而不是只审计数据字段。

### P0-4 可行集合阈值 ε 没有统计语义

原方案写“容差 ε 从开发集决定”。这样得到的 Zε 不是置信集，随之而来的“覆盖率／错误率”只是经验调参结果，审稿人无法把它和任何名义水平对应，风险—覆盖率曲线也失去解释力。

**修复：** 让 ε 由名义水平 α 决定。可选路线：轮廓似然（profile likelihood）阈值 ε = min L + q_{1−α}，q 由校准集上的经验分位数或 χ² 近似给出；或用留出轨迹做分裂 conformal 校准。随之有一个原方案完全没写出的优点：**同一集合对所有策略对同时有效**，k 个策略的 k(k−1)/2 个成对结论共享一个 (1−α) 集合，不需要逐对做多重比较修正；只有 Monte Carlo 误差那一部分需要同时置信带。这应当作为方法的明确卖点写出来。

### P0-5 仿真结构偏差 b 没有估计方法

原方案 5.3 写“若能够界定 b 则扩展区间，没有可辩护的 b 时只报告经验敏感性范围”。这是把最难的问题推给读者。TOP 期刊审稿人一定会问：“你的集合根本不包含真实系统，区间有什么意义？”

**修复：** 明确 b 的两条估计路线并都做。其一，用 A 级配对数据（同一策略、同一任务的仿真成功率与真机成功率）估计差值残差 r = Δ_real − Δ_sim 的分布，以其分位数作为经验 b，这与 Sim2Val 的控制变量思想一致但目标是差值而非绝对值。其二，把“无任何相容参数能解释留出轨迹”做成校准过的拟合优度检验，作为结构失配的显式报警。同时报告“区间实际覆盖率”作为诊断量，明确它是经验量而不是保证。

## P1：显著影响评分

### P1-1 inf／sup 取自噪声估计，存在乐观偏差
Δij(z) 是有限 rollout 的 Monte Carlo 估计，对 z 取最小值会系统性低估下界（optimizer's curse）。VLA 策略自身还有采样随机性。**修复：** 各 z 之间使用共同随机数（相同初始条件种子、相同策略采样种子）；先对每个 z 构造 Δ 的同时置信带，再对下带取 inf、对上带取 sup；参数维度 ≤ 6 时用高斯过程响应面替代逐点评估。

### P1-2 计算量未核算且缺代理模型
参数 × 策略 × 场景 × rollout × 重复，VLA 每次 rollout 需数十到数百步推理。原方案只说“缓存”。**修复：** 明确采用低维响应面 + 主动采样，在 4–6 维 z 上以 64–256 个设计点拟合 Δ(z) 的代理，并在第 3 周用实测吞吐给出全部实验的 GPU 小时表。

### P1-3 RQ3 与经典实验设计的区别没有讲清
“为区分策略排名而选证据”在贝叶斯实验设计里对应决策论效用（期望信息增益 vs. 期望决策价值），在控制论里对应 I4C 的实验设计。原方案只和随机、均匀、熵、误差优先比。**修复：** 把目标函数明确写成“期望可判定策略对数的增量／成本”，加入 D-最优与参数熵作为经典基线，并说明代理模型下的可计算形式。有了 AutoEval 后，可以做真实探测动作选择，不必停留在“离线日志选择”。

### P1-4 缺少现象普遍性研究
原方案只承诺“找到稳定的排名翻转案例”。TOP 期刊需要的是：**这个现象有多普遍、什么条件下发生。** **修复：** 增加“歧义图谱”实验：对 SIMPLER 全部任务 × 策略对，在名义水平 α 的相容集合上统计可判定比例、翻转比例，并与激励失配指标（校准轨迹在 ∇Δ 方向上的信息量）做回归。这是独立于方法的经验贡献。

### P1-5 基线的拒判机制未具体化
原方案强调公平但没说怎么给基线加拒判。**修复：** 逐一定义：单点拟合 + 轨迹自助法得到 Δ 区间；DROPO／BayesSim 后验采样得到 Δ 分布分位数；SCAPE／Sim2Val 用其自身预测区间；纯数据预测器用 conformal 区间。全部用同一 δ 与同一名义水平。

### P1-6 理论命题选择不当
命题 3（单调性）几乎是定义的直接推论，价值低。缺的是解释现象成因的定量结论。**修复：** 替换为“激励失配判据”：若校准数据的 Fisher 信息在 ∇_zΔij 方向上为零（或极小），则任意数量的同分布校准数据都不能使该策略对可判定；给出线性高斯代理下的显式形式，并推导达到 δ-可判定所需的定向信息量下界。命题 1（不可辨识反例）与命题 2（条件正确性）保留。

### P1-7 VLM 模块是负资产
原方案自己承认“若 VLM 不带来可重复收益则去掉”。它既不是贡献，又给审稿人增加攻击面（提示泄漏、可复现性）。**修复：** 主文去掉 VLM 事件提取，事件由几何跟踪产生；“大模型”在论文中只体现为评估对象 VLA。若坚持保留，放附录作为可选接口。

### P1-8 选刊与内容错配
原方案把 Information Sciences 列为“当前资源下的主投候选”，Neural Networks 次之。这两刊的审稿人池不评估机器人 Sim2Real 证据，论文的真机验证在那里既不加分也难被理解；且这类刊物发表机器人策略评估工作的引用与影响力显著低于机器人旗舰刊。方案是在把“能中”放在“该投”之前。**修复：** 主投 IEEE T-RO，备选 IJRR；若 AutoEval／RoboArena 真机证据充分，可评估 Science Robotics（订阅制，需核 OA 政策与单位目录）。T-ASE 作为自动化视角备选。Information Sciences／Neural Networks 从主线移除，只在方法被审稿人定性为“纯统计决策方法”时考虑。所有刊物的中科院分区仍需按单位年度目录核验，原方案这一点是对的。

## P2：细节

- **标题**：英文题过长；中文题写“视觉语言动作模型”而英文题没有对应。建议英文题 *Decision-Relevant Identifiability: When Can Calibrated Simulation Rank Robot Foundation Policies?*，中文题《决策相关可辨识性：校准仿真何时能可靠排序机器人基础策略》。
- **研究问题排序**：应加一条关于普遍性的 RQ（对应 P1-4），并让 RQ 与贡献一一对应。
- **多重比较**：原方案仅在 6.2 提“联合误差控制”。应明确：参数不确定性部分由共享集合自动联合；Monte Carlo 部分用同时置信带；真实标签部分用 cluster bootstrap。
- **时间线**：单人 12 个月做 A–F 六个实验偏紧。应把“机制验证 + 歧义图谱 + SIMPLER 闭环 + AutoEval 探测”列为必做，把 RoboArena 第二平台与 PolaRiS 列为条件项。
- **数据许可与隐私**：RoboArena 视频含评测者与环境；DROID 为 CC-BY 4.0。写入数据使用声明。
- **预注册**：假设、指标、ε 选择程序、δ 取值、冻结的测试集合在 OSF 或等价平台预注册；原方案有“预先登记内部目标”的意思但没有落实到机制。
- **复现包作为贡献**：发布冻结的“歧义案例集”（真实日志片段、相容参数集合、策略对、翻转证据、检查点哈希）是 TOP 期刊看重的资产，原方案只写在附录里。
- **图 1 的设计**：应展示同一段真实轨迹、两个拟合误差相同的物理解释、两条预测视频帧，以及两个策略在两种解释下成功率的交叉。这是全文最重要的一张图，应在第 3 周就产出。

## 原方案中应保留的内容

以下内容是原方案的优点，修订版全部保留并强化：明确区分参数迁移／跨仿真器／真机三种证据；把 ν、κ 与 θ 分开并先核对动作协议；以策略差值代替独立点分数；部分序而非强制全排名；风险—覆盖率定义与公平拒判原则；真实标签三种类型的区分；cluster bootstrap 与独立样本量的说明；数据可行性门槛与停止条件；GPU 分阶段运行与显式核算。

## 本次核验所用来源

- AutoEval：https://arxiv.org/abs/2503.24278 ，https://auto-eval.github.io/ ，https://github.com/zhouzypaul/auto_eval
- RoboArena：https://arxiv.org/abs/2506.18123
- SIMPLER：https://arxiv.org/abs/2405.05941 ，https://github.com/simpler-env/SimplerEnv
- SCAPE：https://arxiv.org/abs/2608.19425
- R2S-Eval：https://arxiv.org/abs/2609.03276
- Real-is-Sim：https://arxiv.org/abs/2504.03597
- 值等价原理：https://arxiv.org/abs/2011.03506
- 面向控制的辨识：https://perso.uclouvain.be/michel.gevers/PublisMig/C128.pdf

**2026-09-18 补充核验结果。**
- SIMPLER 检查点：可用。RT-1-X zip 与 RT-1 converged／15%／begin 三个目录均在公开 GCS 桶中完整存在；Octo base／small 及 1.5 版在 HuggingFace 可访问。REAL_PERF 表含 9 个任务，其中 rt-2-x 只有真机数字、无公开检查点。
- AutoEval 队列：状态存疑。官网写明四个任务开放至 2026-01-01、2026 年待定；提交入口 auto-eval.ngrok.app/page 两次探测均为 ERR_NGROK_3200（端点离线）。旁证显示评测日志数据集最近更新 2026-06-14、代码仓库最近提交 2026-03-26，即今年上半年仍在运行。结论：不能假定当前可提交，须第 1 周联系作者确认。

仍未核验：RoboArena 策略提交的当前流程与门槛；各候选期刊在本单位年度中科院分区表中的字段。
