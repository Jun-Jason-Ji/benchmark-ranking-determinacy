# 面向机器人基础模型的物理歧义感知仿真校准与可信 Sim2Real 评估

研究方案版本：2026-09-18。面向具备 GPU、暂无自有机器人的研究者。默认研究周期为 9–12 个月，指首次投稿，不包含审稿周期。

这是一份待实验检验的研究设计，不是已经完成的实验或录用承诺。论文、数据和期刊政策均按检索时的公开资料整理；尚未下载全量数据或实测代码。文中的任务规模、性能目标和资源预算均为计划值。

## 1. 推荐定位与题目

**推荐中文题目：面向视觉语言动作模型的物理歧义感知仿真校准与可信策略评估。**

**推荐英文题目：When Can Simulation Rank Robot Policies Reliably? Physics-Ambiguity-Aware Calibration from Limited Real-World Trajectories.**

论文中心问题：有限真实轨迹往往不能唯一确定仿真参数。如果多套同样符合观测的物理解释对候选策略给出相反排名，怎样发现这种歧义、暂缓不可靠结论，并选择最有价值的已有轨迹来减少歧义？

本方案属于 **Real2Sim 校准支撑的 Sim2Real 可信评估**。主论文不把公开真实日志验证写成新策略的在线真机迁移。如果后续获得远程机器人或合作实验，再扩展到“选择真实探测动作—更新物理假设—改善真实任务表现”。

大模型的作用有两层：主要评测对象是 VLA 机器人基础模型；可选使用冻结 VLM 提取接触事件、提出物理假设。大模型不直接给出真实质量、摩擦系数或最终成功真值。若 VLM 不带来可重复收益，则保留 VLA 评估主线，去掉辅助 VLM 模块。

## 2. 为什么不能直接做“LLM + 仿真校准”

下列已发表或近期公开工作构成必须比较的近邻：

| 工作 | 已有贡献 | 本项目不能重复声称的创新 |
|---|---|---|
| [DROPO](https://arxiv.org/abs/2201.08434)，2022 预印本／2023 期刊 | 从少量离线真实轨迹估计随机化参数分布 | “首次离线校准”“首次建模参数不确定性” |
| [ASID](https://arxiv.org/abs/2404.12308)，2024 | 主动探索、系统辨识、仿真修正与真实执行 | “首次用少量真实交互纠正仿真” |
| [SIMPLER](https://github.com/simpler-env/SimplerEnv)，2024 | 真实策略的仿真评估、视觉匹配及排名相关性 | “首次用仿真预测真机策略排名” |
| [Sim2Val](https://nvlabs.github.io/sim2val/)，CoRL 2025 | 用配对仿真—真实样本降低真实表现估计方差 | “首次用少量真实样本和大量仿真估计性能” |
| [PolaRiS](https://polaris-evals.github.io/)，2025 | 视频重建场景、逼真仿真与通用策略评估 | “首次从真实视频构建评估场景” |
| [SCAPE](https://arxiv.org/abs/2608.19425)，2026.08 预印本 | 场景条件性能预测、仿真偏差校正与 conformal 不确定性 | “加置信区间／场景留出就构成创新” |
| [R2S-Eval](https://arxiv.org/abs/2609.03276)，2026.09 预印本 | Real2Sim 校准、VLM 偏好评价与策略排名 | “校准 + VLM 打分 + 排名”的简单组合 |
| [BayesDICE / Offline Policy Selection under Uncertainty](https://proceedings.mlr.press/v151/yang22a.html)，2022 | 基于离线后验不确定性的策略选择 | “首次不确定性感知离线策略选择” |
| [Confounding-Robust Policy Evaluation in Infinite-Horizon Reinforcement Learning](https://arxiv.org/abs/2002.04518)，2020 | 在观测约束下研究策略价值的部分辨识与界 | 不能将部分辨识、稳健集合本身说成新概念 |

**拟验证的差异**：将机器人接触与控制参数的观测歧义，显式传播到同一场景、同一参数下的策略差值；输出能复现的“排名翻转证据”；用决策相关的已有轨迹选择减少歧义。最终创新性仍需结合全文和持续文献跟踪确认，不能声称已经证明全球首创。

## 3. 三个研究问题与可证伪假设

**RQ1：低轨迹拟合误差是否足以支撑可靠策略排名？**

H1：存在多套轨迹误差相近的参数，使候选策略排名翻转；因此单点参数拟合或平均轨迹误差不足以判断评估可信度。

测试：在已知真值的受控仿真中系统构造低激励、遮挡、时延与摩擦耦合等条件，比较参数拟合误差、排名差值范围和真实目标环境的排序错误。仅发现几个精挑案例不足以支持结论，需要随机生成与预先冻结的测试集合。

**RQ2：保留物理歧义并允许暂缓排序，是否比强制输出全排名更可靠？**

H2：在相同的有效覆盖率下，歧义感知方法降低错误排序率；在相同错误率约束下，允许评价更多策略对。

测试：绘制风险—覆盖率曲线，明确“覆盖率”是给出结论的策略对占比，不是预测区间覆盖真实值的比例。需要与所有基线加同类拒判机制后的结果比较，不能用“本方法可拒判、基线被迫全答”的不公平设置。

**RQ3：为区分策略排名而选择日志，是否比为缩小全部参数方差而选日志更有效？**

H3：同样的日志数量／标注成本下，决策相关选择比随机、均匀覆盖、参数熵和轨迹误差优先选择更快降低排序歧义。

测试：每场景设定 1、3、5、10、20 条证据预算，报告学习曲线和成本。该实验叫“离线证据选择”，不叫真实主动交互。若只有元数据可提前看，应在所有方法之间统一可见信息；不能先读完整轨迹，再声称只花了被选轨迹的获取成本。

## 4. 最小任务范围

先做短时程刚体操作，避免一开始同时承担布料、液体、长时程规划、三维重建和大模型训练。

| 任务族 | 优先考虑的歧义 | 为什么会影响策略比较 |
|---|---|---|
| 平面推物 | 摩擦、接触点、动作增益、执行时延 | 快推与慢推可能在不同参数下交换优劣 |
| 抓取后移动／放置 | 夹爪接触参数、控制器增益、相机外参误差 | 抓取稳定性与落点误差影响成功 |
| 开合抽屉／铰接物体 | 阻尼、关节摩擦、初始状态 | 较大动作与谨慎动作的表现不同 |

首个原型只做平面推物；第二任务选公开数据和现成场景支持最好的一种。真实任务数量由数据审计结果决定，而不是先承诺所有任务都可重建。

参数从 4–6 维开始：接触摩擦、控制增益、时延，以及必要的初始位姿偏差。质量、惯量、力等只有在输入／标定足以支持时才加入。纯位置控制和未知接触力下通常不能把视觉拟合得到的“等效参数”直接当物理真值。

## 5. 方法设计

### 5.1 数据表示与观测处理

真实证据集记为 D = {(o_t, a_t, q_t, Δt)}，包含图像、动作、机器人本体状态和时间戳；缺失字段显式标记。任务上下文 c 包括指令、场景、机器人型号和控制协议。

将未知量分开：动力学参数 θ、观测／相机参数 ν、控制接口参数 κ。先核对动作单位、控制模式、频率和时序，再研究接触物理。否则相机偏移或动作协议错误很容易被错误吸收到摩擦或质量中。

观测摘要使用机器人轨迹、可跟踪物体位移、接触起止事件、滑移和停止时间。VLM 可辅助事件候选与语义类别，但数值位置尽量来自几何／跟踪，事件标签抽样人工审计。测试结果、偏好标签和政策身份不进入特征提取提示。

### 5.2 保留多组可解释观测的参数

令联合未知量 z=(θ,ν,κ)，建立与真实证据相容的候选集合：

Zε(D) = {z : L_dyn(D,z) ≤ ε，并满足预设物理及标定约束}。

可对观测／控制参数做 profile 优化或边缘化，但不能忽略它们的误差。如果工程上先冻结 ν、κ，后续区间必须明确为“条件于该观测与控制标定”的结论。

起步用多起点优化、随机采样／SMC 和受约束搜索，保留多个模式；另设单点拟合、均匀随机化和 DROPO 风格分布作为基线。参数上下界、容差 ε 和观测噪声尺度从开发集决定。

拟合误差按观测噪声归一化，不能给每种方法随意不同的权重。若使用任务敏感权重，应称为任务加权目标或广义后验，不能自动宣称是正确物理后验。

当所有候选参数都无法解释留出轨迹时，输出“模型结构或观测假设不相容”。不能靠把集合缩到一个错误模型来制造高置信。

### 5.3 以策略差值代替独立点分数

在共同场景分布和共同联合参数 z 下定义：

Δij(z,c) = J_sim(πi;z,c) − J_sim(πj;z,c)。

对相容参数集合求差值下界和上界：

Lij = inf[z∈Zε(D)] Δij(z,c)，Uij = sup[z∈Zε(D)] Δij(z,c)。

实际估计还须考虑有限 rollout 误差与仿真结构偏差。若能够在适用条件下界定差值的偏差 b，则将范围扩展为 [Lij−b, Uij+b]，再加入 Monte Carlo 误差。没有可辩护的 b 时，只报告经验敏感性范围及外部校准结果。

设实践差异阈值 δ：区间下界大于 δ 才输出 πi 更好；上界小于 −δ 才输出 πj 更好；否则暂缓排序。输出部分序关系，不强行补成全排名。

**数学边界**：有限随机参数样本上的最小／最大值只是有限集合敏感性范围，不是整个连续参数空间上的认证界。只有低维可验证例子可以尝试区间分析或分支定界；高维任务须诚实使用经验命名。参数集不覆盖真实系统、残差模型错误或分布改变时，不保证真机排序正确。

### 5.4 排名翻转证据

当两组联合参数 z+ 与 z− 均解释已观察轨迹、但导致 Δij 符号相反时，保存并展示：

- 两个模型的轨迹拟合误差与预测视频；
- 导致策略差异的物理事件，例如滑移、停止时间或接触后回弹；
- 哪类额外证据有机会区分这两个模型。

这既提供可检查的机制解释，也能揭示“低误差但错误选型”的失败。若排名差异来自纯渲染或策略随机性，应分别归因，而非全部称作物理因果。

### 5.5 决策相关的离线证据选择

从符合当前场景／对象身份要求的未使用日志池中，优先选择预期减少策略差值范围或排名翻转数的证据，而不是单纯减少所有参数方差。日志必须来自足够相同的系统；不同物体／日期的参数不能未经建模混在一起校准。

实现路线：用已有证据下的参数候选预测候选动作／元数据对应的可观测结果，计算分歧；经成本归一化选择一条待揭示轨迹，再更新集合。所有选择策略使用同一可见元数据和预算。无法得到候选动作或场景匹配时，降级为已获取数据的子集选择，并只声称节省标注／计算成本。

新增真实动作设计留作增强实验：只有真实执行了算法选择的动作并收集新观测，才称为主动真实探索。

## 6. 理论部分应做到什么

建议提供一个清楚的不可辨识反例和一个有条件的排序正确性命题，而不是包装宽泛的“泛化保证”。

1. 构造两套动力学，对行为策略已覆盖的动作产生相同观测分布，对目标策略产生相反价值差。说明仅凭这些离线证据无法唯一确定策略排名。这是既有部分辨识思想在接触机器人中的具体化，不能声称理论概念首创。
2. 在真实环境由集合内某参数表示、模拟收益估计误差和模型差值偏差受到有效控制的条件下，证明扩展区间不跨过 δ 时的排序结论正确，并说明多个策略对的联合误差控制方式。
3. 在增加相容证据只缩小集合的理想条件下，证明真实极值区间不会扩大。实现这一前提需要逐批置信约束交集或显式嵌套集合；新增数据后重新拟合“整体平均损失 ≤ 固定阈值”不保证集合嵌套。连续多次更新还需处理联合覆盖。近似采样算法可能不满足单调性质，必须分别讨论。

创新主要要落在可计算的结构化机器人方法、证据选择和外部真实验证上，不能仅靠上述基本命题支撑整篇论文。

## 7. 数据与平台：已确认的内容及不能假定的内容

| 来源 | 公开确认的内容 | 本方案用途与限制 |
|---|---|---|
| [SIMPLER 原始官方仓库](https://github.com/simpler-env/SimplerEnv) | 仿真环境、指标代码和真实策略聚合结果；另有控制器辨识工具说明 | 第一平台和历史外部验证。聚合成功率不是逐次真机日志 |
| [SIMPLER 新环境说明](https://github.com/simpler-env/SimplerEnv/blob/main/ADDING_NEW_ENVS_ROBOTS.md) | 从已有轨迹做动作回放和控制器参数辨识的流程 | 复现控制器校准基线，核实可下载匹配轨迹 |
| [RoboArena 2026-07-17 快照](https://huggingface.co/datasets/RoboArena/DataDump_07-17-2026) | 官方卡列 3,883 个 session、10,783 个策略 episode；视频、本体状态／动作、结果与偏好元数据 | 核心真实外部证据来源。没有自动配套每条轨迹的仿真数字孪生 |
| [PolaRiS 代码](https://github.com/arhanjain/polaris) 与 [环境](https://huggingface.co/datasets/owhan/PolaRiS-Hub) | 重建场景、初始条件、任务评分定义和部分可运行策略检查点 | 第二仿真平台。环境评分定义／运行后生成的 CSV 不等于已公开真机 episode 标签 |
| [DROID](https://droid-dataset.github.io/droid/the-droid-dataset.html) | 真实示范、多视角视频、动作和机器人状态，提供小规模调试子集 | 感知与动作接口调试。示范的 reward 编码不能当作候选策略部署成功标签 |
| [GAUGE](https://arxiv.org/abs/2608.05948) | 真实测量物理任务；工具发布状态需继续核查 | 备选物理诊断，不作为必须依赖的唯一执行平台 |
| [RoboDojo](https://robodojo-benchmark.com/) | 官方介绍远程真实机器人评估系统 | 可争取增强验证；访问资格、费用、排队和能否修改物理条件尚未确认 |

SIMPLER 的官方 REAL_PERF 数据可提供少量 task×policy 比较，但其中策略不一定都有可重跑检查点。不能将不可获取策略纳入“全部复现”。

RoboArena 的不同时间快照是累积数据，做时间留出必须先按 session ID 去重，再用真实时间戳划分，不能把两个快照直接当独立训练／测试集。多视角视频属于同一个 episode，不能计作多个独立样本。

**关键接口风险**：PolaRiS 的共训练检查点不必然等于 RoboArena 历史评估中的原始检查点。必须匹配模型权重版本、动作空间、控制频率、机器人和初始条件。若无法匹配，就不能声称进行了同一策略的 sim-real 成对比较。

## 8. 首月必须通过的数据可行性门槛

先抽查 50–100 个真实 session，不必先下载全部视频。制作字段审计表：场景／站点 ID、任务、策略权重版本、动作定义、时间戳、标定、成功标签、视频可读性、能否重建。

将样本分三级：

- A 级：环境可匹配、策略可重跑、接口与结果可靠，支持严格闭环仿真—真实比较。
- B 级：有动作和可跟踪视频，但缺少检查点或完整重建；仅支持动作回放／动力学预测诊断。
- C 级：只有视觉或聚合结果；只作辅助分析。

选题继续条件：至少找到一个可用主环境、3–5 个可复现策略和独立真实评价来源；完整期刊实验尽量扩展到 4–6 个任务、多个独立场景组。以上为项目门槛建议，不是当前已确认覆盖。

如果一个月内无法得到足够 A 级数据，保留仿真机制研究，但必须选择以下一条：获得数据作者协作或远程评测；或者把论文题目改为真实轨迹约束下的动力学歧义分析，不再把真实闭环策略排名作为主要结论。没有自有机器人可行，没有足够真实证据则不能承诺完成强 Sim2Real 论文。

## 9. 实验设计

### 实验 A：受控机制验证

建立约 3 个任务族、每族 10–20 个参数情境；最终数量根据功效与计算预算调整。用已知目标参数生成独立轨迹，控制激励不足、遮挡、控制时延和接触参数耦合。比较单点拟合、参数分布与本方法在识别排名翻转方面的能力。

同引擎不同参数只称参数迁移；如增加不同引擎／不同接触模型，称跨仿真器或结构失配实验。二者都不是现实机器人证据。

### 实验 B：公开真实轨迹拟合与留出预测

真实校准轨迹与真实留出轨迹按 session／对象／站点分开。在校准时不读取留出结果。评测事件时间、物体轨迹和机器人状态预测，验证集合是否识别了无法解释的新轨迹。

参数恢复误差只在有真值的合成部分报告。真实部分报告可观测量预测误差与不确定性，不把不可核实的参数估计当准确率。

### 实验 C：真实策略评估有效性

只对 A 级数据与可复现策略构造匹配比较。优先选相同场景、相同任务分布下的策略；按 session 分层分析。固定物理校准集，冻结方法后再打开真实测试结果。

需要区分三种真实标签：任务成功／进度、人工偏好、全局评分。不能把人工偏好等同于成功率，也不能用跨任务全局排行榜代替同任务排名。模拟初始条件分布必须尽量匹配真实评测；做不到时单独报告分布不匹配。

### 实验 D：泛化与失配

单独测试未见场景、物体、策略家族和时间段，最后才做组合留出。控制不同训练规模，不把同一模型的相邻 checkpoint 当独立模型家族。

有意加入候选参数空间不能解释的现实差异，例如额外时延、接触模型改变或相机漂移，检验方法是否变得不确定，而非错误地保持自信。任意 OOD 可靠性只能实测，不能由常规 conformal 自动保证。

### 实验 E：离线证据选择与消融

相同预算下比较随机、均匀覆盖、误差优先、参数熵优先、策略差值分歧优先。重要消融包括：单点／多模态集合、动力学／视觉参数分离、同参数差值／独立分数区间、拒判机制、VLM／几何事件提取、错误 VLM 先验。

### 增强实验 F：远程／合作真机

如果平台允许，在 2–3 个任务上固定候选策略与有限探测动作，比较无需额外交互、随机探测、参数辨识探测和决策相关探测。报告真实交互次数、成功率、恢复成本。任务与动作授权、硬件条件和数据使用方式须事先明确；本方案没有联系任何团队或提交远程任务。

## 10. 基线与公平比较

最低应包含：默认仿真；标准域随机化；单点 SysID（CMA-ES／退火等）；DROPO／BayesSim 类参数分布；SIMPLER 原评估；纯数据结果预测器；以及能符合数据前提的 Sim2Val／SCAPE。

R2S-Eval 代表 VLM 偏好评估路线，应做相关方法比较；若官方代码或所需数据不可用，可按论文复现并清楚标为自行实现，不能给它使用更多真实标签或不同任务后直接比较绝对数值。

BayesDICE 等离线策略评估作为概念近邻；只有其状态、动作概率、覆盖等条件适用时才纳入主数值比较。不能把强方法放进明显不适用的数据设置当作弱基线。

匹配真实证据数、标注成本、仿真调用数、策略版本、预训练信息和超参数搜索预算。对于依赖真实 outcome 的方法与只依赖动力学轨迹的方法，设置两条独立赛道并明确数据权限，不能混为相同监督预算。

## 11. 指标、统计与证据规模

主指标：

1. 固定覆盖率下的错误排序率，以及风险—覆盖率曲线面积。
2. 固定错误率阈值下可比较的策略对比例。
3. 选型遗憾值：最优真实策略与选中策略的真实表现差；拒判场景须报告对应处理成本，不能当零损失。
4. 达到同一可靠度需要的真实证据／标注成本。

辅助指标：Kendall τ、MMRV、成功率／进度预测误差、区间宽度与实际覆盖、事件／轨迹误差、GPU 时间与仿真调用数。Spearman/Pearson 在只有几个策略时不稳定，不单独作为论文结论。

真实测量也有不确定性：只有有限执行次数、评分噪声时，某些真实策略对本身不可区分，应标为未决。对 session／场景做 cluster bootstrap；同一 episode 的帧、同一 session 内策略比较不是独立样本。真实结果只是聚合均值且缺少样本量时，不生成伪造 episode 来做 bootstrap。

规划 3–5 个随机种子，按实际独立性解释；策略对数量不能被当作独立环境样本量。仿真先用少量 rollout 淘汰不可行方法，再对主比较进行 50–100 次／条件的预算起点，是否足够取决于方差、差异大小与置信区间。

参考量级：若真实成功率约 0.5，独立二项试验 n=20 时，正态近似 95% 半宽约 0.22；n=100 时约 0.10。正式报告用 Wilson／适当模型区间。少量真机样本不能有力区分几个百分点的改进；增加仿真样本不等于增加独立真实证据。

预先登记的内部目标可设为：在覆盖率 70% 时，相对最强适用基线将排序错误率降低约 20%；或达到相同错误率使用更少证据。它只是项目推进指标，不是预期必达结果，也不是该期刊的录用门槛。效应太小、置信区间重叠明显时必须如实报告。

## 12. GPU 与工程预算

本方案以推理和低维校准为主，不从零训练视频世界模型或大型 VLA。PolaRiS 官方说明其一个 π0.5 推理加环境示例在 24 GB RTX 3090 上测试可用；这不代表所有模型、分辨率和并行数都能如此运行。[官方代码说明](https://github.com/arhanjain/polaris)

起步配置：单张 24 GB 级 GPU、64 GB RAM、足够容纳筛选子集的 SSD；双 GPU 可分别用于策略推理与仿真，但不是先决条件。16 GB 及以下需缩小模型与渲染规模，实际先做适配测试。建议独立 Linux 实验环境；不因本方案修改现有系统或启动配置。

校准、仿真评估和 VLM 事件提取分阶段运行，避免争抢显存。缓存状态摘要、策略输入和仿真参数，不重复渲染全部视频。低成本任务可用 32–64 组参数做原型，正式规模按误差收敛曲线决定。

总运行量必须显式核算：参数数 × 策略数 × 场景数 × rollout 数 × 校准预算 × 重复次数。用少量完整试跑测得吞吐，再估计全部消融所需 GPU 小时。不能在没有基准运行前给出确定云成本或总天数。

## 13. 进度与停止条件

| 阶段 | 时间 | 交付与继续条件 |
|---|---|---|
| 数据与文献审计 | 第 1 月 | 可复现策略—真实标签—可匹配场景矩阵；确认主平台和期刊候选 |
| 复现基础评估 | 第 2 月 | 一个任务跑通默认仿真、单点校准和参数分布；复核动作协议 |
| 建立机制证据 | 第 3 月 | 冻结反例集合，发现稳定排名翻转；不是仅单个演示 |
| 完成核心方法 | 第 4–5 月 | 多参数集合、差值评估、暂缓排序、证据选择及关键消融 |
| 真实外部验证 | 第 6–7 月 | A 级真实证据；按场景／策略家族留出；完成最接近方法比较 |
| 增强与稳健性 | 第 8–9 月 | 第二平台、失配实验；若可行开展远程真机 |
| 写作与复现包 | 第 10–12 月 | 冻结测试、整理局限、完成稿件与补充材料、首次投稿 |

关键停止／转向条件：首月无法获得 A 级数据则调整真实结论；第三月找不到广泛且稳定的物理歧义导致排序问题则不继续堆模型；第五月收益仅来自拒判或额外预算则重做公平比较；VLM 无增益就删除；只在一个场景／一个策略家族有效则降低泛化主张。

如果必须六个月投稿，应收缩到两个任务族、一个主平台与一个真实外部数据源，并取消大型模型训练与在线主动探索；不能同时维持全部范围和完整顶级期刊证据强度。

## 14. 论文应提供的关键图表

图 1：同一真实观测对应两种可行物理模型，但策略排名相反。

图 2：从真实证据到物理集合、策略差值、部分排名和证据选择的流程。

图 3：轨迹误差与排序错误的关系，展示二者并不等价。

图 4：公平拒判设置下的风险—覆盖率曲线。

图 5：1／3／5／10／20 条证据的预算收益曲线。

图 6：跨场景、策略家族和时间留出的真实验证，以及结构失配时的失败案例。

主表：数据分割与独立样本量、基线结果、核心消融、真实标签不确定性、运行成本。附录：参数范围、优化细节、训练和测试权限、检查点哈希、重建质量、失败案例及完整命令。

## 15. 投稿定位与非 OA 路线

本方案以中科院 1 区或 2 区 TOP 档次的研究质量为目标，但具体刊物是否满足单位要求，需要核对单位使用的年度、大类／小类和 TOP 字段。公开检索没有获得所有候选的官方分区记录，不能把 JCR Q1 或影响因子当作中科院分区证明。正式选刊通过[中科院分区表官方入口](https://www.fenqubiao.com/)和本单位目录确认。

“非 OA”在这里解释为选择期刊的传统订阅发表路径；Hybrid 期刊允许这种选择。它不等于完全没有任何版面、超页或彩色费用。若单位连 Hybrid 期刊也排除，需要重新筛选。

以下是适配性候选，全部需要先通过单位分区目录筛选；没有把任何未核验刊物标成“已确认中科院 TOP”。

| 候选期刊 | 建议定位 | 与本方案的适配条件 | 非 OA 政策核查 |
|---|---|---|---|
| Information Sciences | 当前资源下的方法型主投候选 | 突出不确定性决策、可信排序、证据选择；具有独立算法贡献 | [Elsevier 官方页面](https://shop.elsevier.com/journals/information-sciences/0020-0255)确认提供订阅；作者端最新指南抓取受限，投稿前仍须确认 Traditional/Subscription 选项和费用 |
| Neural Networks | 学习算法型主投候选 | 神经后验表示／排序学习必须是中心贡献；不能仅将 VLM 作为接口 | [Elsevier 官方页面](https://shop.elsevier.com/journals/neural-networks/0893-6080)确认订阅，并明确神经网络须是论文核心；作者端条款需最终核查 |
| IEEE Transactions on Automation Science and Engineering | 自动化方向重点候选 | 证明减少真实测试成本、改善自动化系统策略选择；需完整真实证据 | [官方作者指南](https://www.ieee-ras.org/publications/t-ase/information-for-authors-t-ase/)明确 Hybrid，OA 可选，存在传统发表路线 |
| IEEE Transactions on Robotics | 增强验证后的冲刺候选 | 较强机器人方法创新、机制解释、多场景证据；远程／合作真机验证会明显增强论文 | [官方作者指南](https://www.ieee-ras.org/publications/t-ro/t-ro-information-for-authors/)提供 Hybrid／传统路径；超页费用另计 |
| The International Journal of Robotics Research | 深入系统研究的冲刺候选 | 形成有普适价值的机器人科学结论，理论与实证均充分 | [官方指南](https://journals.sagepub.com/author-instructions/ijr)标明 Subscription，Sage Choice OA 为可选 |

选刊顺序不应固定为盲目逐级投递：若结果主要是决策与统计方法，优先核定 Information Sciences；若形成新神经学习方法，考虑 Neural Networks；若自动化部署证据最强，考虑 T-ASE。T-RO／IJRR 需要按最终成果判断，不因题目包含大模型和 Sim2Real 就自动适合。

如果要求所有候选都必须已经从官方作者指南确认可走非 OA，当前优先保留 T-ASE、T-RO、IJRR；前两本 Elsevier 刊物暂列作者端条款待核实候选。它们的中科院资格仍需独立核定。

不默认推荐 Robotics and Computer-Integrated Manufacturing：其[官方范围](https://shop.elsevier.com/journals/robotics-and-computer-integrated-manufacturing/0736-5845)重视工业相关机器人与制造应用，普通家庭仿真与排序分析的适配较弱。RA-L 可以容纳紧凑机器人贡献，但本方案完整内容较长，且其 TOP 状态同样未核定，因此不作为默认主投。

## 16. 前四周的具体工作清单

第 1 周：固定文献版本，阅读 DROPO、ASID、SIMPLER、SCAPE、R2S-Eval 和 BayesDICE；建立逐项差异表；核查 GPU 显存；抽查 RoboArena session 元数据。

第 2 周：跑通一个 SIMPLER／PolaRiS 环境及一项公开策略；确认版本与动作单位；核查真实数据能否与该策略／场景匹配。

第 3 周：单点参数校准与多组参数搜索；在推物任务中生成可重现的排序翻转例子；记录全流程 GPU 时间。

第 4 周：完成第一张风险—覆盖率曲线和第一份真实数据审计报告；决定继续主方案、寻求远程评测，还是收缩为动力学歧义研究。

最有价值的第一项成果不是先训练一个大模型，而是确认：**在可复现实验里，确实存在“拟合得同样好，却选出相反策略”的问题，而且能够获得独立真实证据检验它。**
